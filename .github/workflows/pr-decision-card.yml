name: PR decision card

# 安全边界：该 job 只接受目标为 default branch 的 PR，并 checkout 对应
# base SHA；不得 checkout 或执行 PR head。PR 正文按不可信数据解析。
on:
  pull_request_target:
    types:
      [
        opened,
        edited,
        synchronize,
        reopened,
        ready_for_review,
        converted_to_draft,
      ]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: pr-decision-card-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  decision-card:
    if: github.event.pull_request.base.ref == github.event.repository.default_branch
    name: nontechnical decision card integrity
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout trusted base
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          persist-credentials: false

      - name: Pin trusted Node runtime
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22

      - name: Render current decision card
        run: >-
          node scripts/pr-decision-card.mjs render
          --event "$GITHUB_EVENT_PATH"
          --output "$RUNNER_TEMP/pr-decision-card.md"
          --result "$RUNNER_TEMP/pr-decision-card-result.json"

      - name: Create or update the bot-owned card
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          CARD_PATH: ${{ runner.temp }}/pr-decision-card.md
        with:
          script: |
            const fs = require("fs");
            const body = fs.readFileSync(process.env.CARD_PATH, "utf8");
            const marker = "<!-- codex-nontechnical-decision-card:v1 -->";
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const issue_number = context.issue.number;
            const comments = await github.paginate(
              github.rest.issues.listComments,
              { owner, repo, issue_number, per_page: 100 },
            );
            const existing = comments.find(
              (comment) =>
                comment.user?.login === "github-actions[bot]" &&
                comment.body?.startsWith(marker),
            );
            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body,
              });
            }

      - name: Reject an untrusted ready merge declaration
        run: >-
          node scripts/pr-decision-card.mjs check
          --result "$RUNNER_TEMP/pr-decision-card-result.json"
