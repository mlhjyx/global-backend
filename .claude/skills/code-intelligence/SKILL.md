---
name: code-intelligence
description: Query the repository-native ContractGraph before cross-module impact analysis, then verify every material conclusion against current source, tests, and runtime evidence.
---

# Repository Code Intelligence (Claude Code entry)

The single source of this skill is [`.agents/skills/code-intelligence/SKILL.md`](../../../.agents/skills/code-intelligence/SKILL.md).
Read that file in full and follow it exactly; this file only makes the skill discoverable to Claude Code.

It is a pointer rather than a symlink because the production dependency source policy does not admit repository symlinks.
If the `description` above ever diverges from the source file's front matter, the source file wins.
