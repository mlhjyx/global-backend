{
  "name": "@global/site-renderer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "SiteSpec(04 契约) → 静态站渲染器：Astro 构建，主题 token 驱动，组件封闭枚举",
  "scripts": {
    "build": "astro build",
    "dev": "astro dev"
  },
  "dependencies": {
    "@global/contracts": "workspace:*",
    "astro": "^5.7.0"
  }
}
