# status/current —— 当前真值与待拍板（活文档）

> 文档 ID：`DOC-STATUS-001`
> 生命周期：`CURRENT`
> 当前事实来源：已合并提交、当前代码与专项验证；历史见下方链接。
> 2026-07-10 立；**2026-08-02 获客开发冻结解除**。这是当前状态权威；历史见 [../roadmap/changelog.md](../roadmap/changelog.md)。
> 下方「真实具备/缺口/偏差/待拍板」四节仍是**获客侧 2026-07-13 冻结时点的历史快照**，解除冻结不自动把旧验证升级为当前验收。测试数量随提交变化，不在活状态文档固化；是否通过以最新分支实际命令、服务证据和 CI 结果为准。

## 🟢 Site Builder M1 已收口；获客侧待选择首个恢复任务

- **2026-08-05 Copy closed-repair operational proof（当前交付分支，zero-call）**：统一 Model Runtime 已把此前硬编码为 0 的内容 repair 落成最多一次的 closed repair；repair plan 必须保持 execution/task/alias/protocol/reasoning/schema/sampling 不变，并逐字绑定 original input/context、prior output digest 与闭合 findings digest，validation-failed 的 prior output 正文不会重新进入 prompt。Durable Runtime 对每条物理 wire 先写 request/usage/output observation，再运行 Copy 固定任务绑定与固定产物集合的 branded compiled-artifact guard；guard 漂移会在保留已知 settlement 后冻结 campaign，unknown settlement 则不允许 repair。Copy 的 loopback fake-gateway runner 新增 `OPERATIONAL_PROOF_ONLY` 私有品牌回执，明确保持 `fake_gateway_contract_only`，不能进入真实质量 evidence 或 promotion。本分支不读取 `.env`/凭据、不连接 new-api/OpenOx、不调用真实模型、不改 active route；#314 的 v1 manifest 固定旧源码，合并本分支后必须保留为历史并另开 create-only v2 固定新 merge commit。真实 dispatch 仍缺 purpose-specific 有限凭据、durable authorization reservation、request-bound new-api consume-log settlement/real ledger factory、v2 fixed source 与单独调用授权。

- **2026-08-05 Copy capability pilot post-merge manifest（当前交付分支）**：#313 已合并；独立 [create-only manifest](../evidence/site-builder/m1-g-copy-real-capability-manifest-v1.json) 现固定该合并提交 `5287e5c4d95cd1eb78a61e71ba84c1dfa56b632a`、59 个 tracked source blob、source bundle `308c6400…`、v3 pilot 合同及 Terra=Responses/medium、Sol=Responses/high、Sonnet=Messages/medium 的精确 3 executions / 最多 6 physical wires / 每次最多一次 repair。生成器逐文件比较工作树与 fixed-commit blob，并要求该提交同时可由准备 HEAD 和 `origin/main` 到达；产物仍为 `FIXED_SOURCE_CREATE_ONLY`、`dispatchAuthorization=NOT_AUTHORIZED`、`dispatchCapable=false`、网络/模型调用和 CNY/USD 实际费用均为零。它不含凭据、网关 client、reservation 或 settlement receipt，也不证明 compiled runtime；真实 dispatch 前仍须重新核验源码并完成 compiled runtime attestation、purpose-specific 有限凭据、可信 settlement/ledger factory、post-wire freeze、repair payload binding 与单独调用授权。真实 evidence、晋级和 route adoption 均未发生。

- **2026-08-04 Model Runtime 重构（当前交付分支）**：ADR-023 已把模型职责重新分层：new-api 只做唯一聚合出口；应用内 Runtime 管 task/context/capability/reasoning/cache/retry/repair/validation/settlement；Langfuse 仅作可选的 OTel 脱敏旁路。Site Builder 保留七个账本 task ID，但 production generative set 只有 `brand_profile`、`copy`，其余五个任务确定性执行且不再进入模型评测。旧 fee-card/native runner/fixed-digest 执行链已删除，最终 `diagnostic_only` evidence 保留。AI SDK 7 的 Responses/Messages adapter 只完成 fake-gateway 协议 PoC；本分支未调用真实模型、未改 new-api/凭据、未 promotion 或切生产 route。

- **2026-08-04 M1-g 诊断执行与阶段结论**：单一 campaign 完成 206/206 executions 与 262 physical wires；机器产物见 [`m1-g-text-evaluation-real-evidence-v1.json`](../evidence/site-builder/m1-g-text-evaluation-real-evidence-v1.json)。复审后该产物已固定为 `diagnostic_only`、`promotionEligible=false`：62 accepted、99 validator rejected、45 transport failed；网关/传输失败不再计作模型质量失败，整体质量结论为 `inconclusive`。它没有 canonical fixed-HEAD/cost-safe execution provenance、digest-bound resolved upstream/channel 或逐 wire settlement，故任何候选都不可据此 rank/promotion。本轮不切 active route、不安装 runtime token、不恢复 MiniMax/Doubao，也不在仓库继续维护此次运行的价格计算；一次性 runner 删除。12 视觉集、确定性发布门与“不晋级、保留现役路由”的 M1-g 决策已闭合，M1 阶段完成；MODEL-2、30+ 成熟系统集、独立审美 Gold、生产部署和 M2-PUBLISH 仍明确未完成。

- **2026-08-04 全部文本 task 的 zero-call suite admission**：`copy`、`assemble`、`assembly_fix`、`qa_summarize` 与 `seo_review` 均已有独立 canonical evaluation suite。copy 由生产 Claim/slot canonicalizer 与 `finalizeCopyBundle` digest 判分；assemble/assembly_fix 各覆盖 M1-e-B 六 Family × sparse/rich 的 12 个 golden input，任务输入冻结每个必选 page/section target，空 selection 或漏选不能通过，再经 selection→SiteSpec→`validateControlledAssembly` 闭合；repair 另固定 prior candidate digest 与 production validator findings。其 source bundle 还逐项绑定 renderer component-qualification JSON fixture。QA/SEO 各冻结 2 fixtures × 2 repeats 的 production quality-narrative contract。所有 suite 的 legacy comparator allowlist 为空；未来真实 dispatch 仍由 `site-builder-model-evaluation-cost-safety/2026-07-30-v2` 在 client 前执行 fixed-commit、有限凭据、精确 scope、冻结价格与 known settlement 门。本阶段不读取凭据、不调用模型、不产 evidence、不改变 promotion、active route 或 runtime fallback；每个任务仍须分别完成 fixed-commit manifest、费用卡、有限凭据/known settlement、真实 evidence 与 promotion。
- **2026-08-04 Copy Evaluation v2 评分器与候选 v2（当前交付分支）**：六场景 × 两次重复的 Copy 输出先过生产 schema/Claim/禁用断言/字符预算/CTA 硬门，再以闭合 finding code 盲评语言、品牌语气、CTA 与跨 locale 质量，并对两次已验证输出确定性计算稳定性；评审模型必须与候选供应商分离，精确 fixture 覆盖不能用重复样本凑数。候选/协议/reasoning/无缓存/known settlement/output digest 只接受 Model Runtime 的品牌完成元数据，repeat 0/1 的 execution/output digest 与深冻结评分共同进入聚合门。`copy.premium` 已由 Terra/GPT-5.5/Sonnet 重基为 Terra/Sol/Sonnet，candidate admission 为 `READY`；pilot 固定 Terra=Responses/medium、Sol=Responses/high、Sonnet=Messages/medium，三者均须先过 `capability_probe`。dispatch 仍为 `NOT_AUTHORIZED`。本分支零真实调用、零 evidence、零晋级、零路由变化；capability pilot、真实矩阵、promotion 和 runtime adoption 继续分别决策。
- **2026-08-03 模型评测源码/manifest 拆分**：`site-builder-model-evaluation-harness/2026-08-01-v18` 与 `design_spec` suite/source bundle v15 已随 #266 合并；矩阵仍为 73/146，单 wire 20¢ 与 execution 40¢ 分层核验，probe attestation 同样按 execution reservation 复验。真实调用、费用、evidence、promotion 与 active route 均未完成；所有文本 task 的 zero-call suite admission 见本页 2026-08-04 记录。
- **2026-08-04 `design_spec` v6 current-source manifest（zero-call）**：#282 合并后的 [manifest v6](../evidence/site-builder/m1-g-design-spec-evaluation-manifest-v6.json) 固定 `origin/main@5c37bb9270db6893144f07c2431e74a830d6b9f4`、v15 source bundle `c6deda…`、73 executions / 146 wire calls、24 个 deterministic comparator case 与 post-build compiled-contract attestation；prepare contract 仍为 `site-builder-design-spec-evaluation-manifest-prep/2026-08-03-v2`。它没有读取 OpenOx/凭据/余额/`.env`，模型和网络调用、实际费用均为零，`dispatchAuthorization=NOT_AUTHORIZED`；v1–v5 继续仅供审计，不能作为此后 dispatch 输入。
- **2026-08-04 `design_spec` v6 公开价格卡（zero-call）**：唯一当前 [v6 price card](../evidence/site-builder/m1-g-design-spec-v6-native-fee-card-2026-08-04.json) 在已固定的 v6 manifest 上读取 OpenOx 公共目录一次，响应 SHA-256=`47063ab8…`；它将 73/146 的最大 token envelope 冻结为 CNY `11.276659` + USD `3.45842784`（不换汇），并记录 `modelWireCalls=0`、实际模型费用 CNY/USD 都为 `0`、`dispatchAuthorization=NOT_AUTHORIZED` 和 `READY_FOR_CREDENTIAL_ATTESTATION`。执行入口只接受这张 v6 卡的受信任 attestation，旧 v5 attestation 在 client/ledger 前拒绝；这不是预计消费或执行授权。下一门仍是 purpose-specific 有限凭据、精确 alias/protocol scope、可验证余额/限额、known settlement 与新的任务级费用授权；任何一项缺失均禁止真实 dispatch。
- **历史 `design_spec` v1–v5 manifests 与公开费用卡**：它们继续保留为可审计的零调用产物；冻结的旧 source、相同的原币上限、`NOT_AUTHORIZED` 和零实际费用都不能跨 source 复用于 v6。当前只认 v6 manifest、v6 公开价格卡及其执行前置校验；随后才可能进入 credential attestation 门。
- **2026-08-04 remaining-text v2 current-source manifest（zero-call）**：唯一当前 [manifest v2](../evidence/site-builder/m1-g-remaining-text-evaluation-manifest-v2.json) 固定 `origin/main@a04f60f5597762d8fde634552b3be6a8a42c8d1d`，逐文件绑定现行 source bundle。它冻结 copy `13/26`、assemble `48/96`、assembly_fix `48/96`、QA `12/24` 与 SEO `12/24`，合计 133 executions / 266 maximum wire calls；只有 manifest 内的精确 alias+protocol 可进入后续独立价格门。它没有读取 OpenOx、凭据、余额或 `.env`，模型/网络调用与实际模型费用均为零，`dispatchAuthorization=NOT_AUTHORIZED`。
- **2026-08-04 remaining-text v3 public-price cards（zero-call）**：五张卡都基于当前 v2 manifest、OpenOx 公共目录和原币 20¢/wire 上限生成：copy CNY `2.177043` + USD `0.58434768`、assemble CNY `8.346384` + USD `9.06492384`、assembly_fix CNY `8.374896` + USD `9.10084896`、QA CNY `0.4903624` + USD `0.50120784`、SEO CNY `0.5030696` + USD `0.51576336`（均不换汇）。所有 candidate 的 initial/repair wire 都未超 cap，五任务均为 `READY_FOR_CREDENTIAL_ATTESTATION`，但仍是 `NOT_AUTHORIZED`、零模型调用和零实际费用。下一门是逐任务 purpose-specific 有限凭据、精确 alias/protocol scope、余额/限额和 known settlement；不得以 new-api 价格、估算或替代候选绕过。旧 manifest、卡和 price block 仅保留审计。MiniMax、Doubao、Gemini 文本、图片、视频、route change、promotion 与 runtime token 仍不在本阶段范围内。

获客侧 **2026-07-13 至 2026-08-02 暂停新增开发**；用户已提前解除冻结，且 Site Builder M1 于 2026-08-04 收口。当前下一步是重新审计并选择获客侧首个实现任务；首项、owner 与验收提交尚未选择，旧分支、旧 worktree、旧 provider 状态和历史验证不得自动复用。

- **M0 与兼容边界 as-built**：注册引导、`Site`/`SiteVersion`/`BuildRun`、素材上传与 KB、Demo v0 Temporal 工作流、Astro 渲染器（55 型组件，11 个 theme preset）及 7 个既有有界 AI Task 路由继续存在。`@global/contracts` 的 `SiteSpec` 已演进为显式 `1.0.0 | 1.1.0` 联合；Demo v0 固定生产 1.0，M1-e-B refurbish 新历史生产 1.1，Renderer 与 Preview 双读，旧 ReleaseManifest v1 不回填。
- **模型网关当前真值（2026-08-03）**：Ubuntu 开发 new-api 已建立按能力域跨供应商划分的三把模型白名单令牌：文本 23 个、图片 4 个、视频 5 个。#140 已合并，自托管 BGE-M3 继续通过私有别名 `site-builder-bge-m3-local` + 专用模型受限令牌由应用统一经 new-api 调用；真实 `EmbeddingsClient` 两条输入均返回 1024 维有限向量，令牌 `/v1/models` 只返回该别名。应用尚未使用新分域令牌，历史 `MODEL_GATEWAY_KEY` 仍是宽权限令牌，故端到端凭据隔离尚未完成。只有 `brand_profile` 是 `gpt-5.6-terra`（Responses）→`claude-sonnet-5`（Messages）的代码级 `promotedRoute`；`design_spec` 已在模型 client 前转为 `safe-blueprint` 确定性安全蓝图，copy 的回退仅保留 GLM，QA/SEO 不再有模型回退，assemble/assembly_fix 保持非退役历史模型路线。通道/型号在令牌 allowlist 可见只证明被允许，不证明当前健康、任务质量、上线或生产部署。
- **现役付费模型结算前置门（2026-08-03 当前交付分支）**：七个 Site Builder task 的 durable structured call 新增短期 digest-bound runtime attestation；它把精确 task/alias/protocol、有限额度且精确 allowlist 的应用凭据指纹、渠道与 OpenOx 模型广场公开目录的冻结价格绑定，并在 durable reserve 与 generative client 前通过只读端点复核。new-api 因未配置用户价格，只承担 request/channel/token/scope 证据，不承担金额真值；每次物理调用以 `x-oneapi-request-id` 解析 token-scoped consume log 后，金额按冻结 OpenOx input/output rate 记 `token_pricing`。最多一次 closed repair 的两条 observation 必须全部匹配，任何 missing/ambiguous/model/channel/price mismatch 均保守 charge reservation、记录失败、冻结该 BuildRun 后续付费调用并抛 `PaidOperationUnknownError`，不得 fallback 重复花费。MiniMax/Doubao 已在模型 client 前从 active route 移除，运行时恢复与 allowlist 均拒绝这些 retired alias；QA/SEO primary 的精确价格与最终 active alias 价格覆盖仍未闭合，完整 runtime attestation 不可安装且模型 dispatch 未获授权。该交付未晋级模型、未调用模型；操作合同与决策门见 [model settlement preflight](../site-builder/model-settlement-preflight.md) 与[退役治理卡](../evidence/site-builder/m1-g-text-route-retirement-decision-card.md)。
- **OpenOx 分域渠道开发配置（2026-07-27，未上线）**：四个上游渠道分别冻结 GPT 图片（`gpt-image-2`、`gpt-image-2-4k`）、Gemini 图片（`gemini-3-pro-image-preview`、`gemini-3.1-flash-image-preview`）、Gemini 文本（`gemini-3.1-pro-preview`、`gemini-3.5-flash`）与 GPT 文本组合（`gpt-5.4`、`gpt-5.4-mini`、`gpt-5.5`、`gpt-5.6-luna/sol/terra`）。组合密钥返回的重叠 `gpt-image-2` 已从文本渠道排除，统一由图片专用密钥承载；`gpt-5.3-codex` 不在该密钥的真实 `/models`，未配置。Gemini 文本目录可见但两次 OpenAI Chat 入口均返回 `openai unified upstream not configured`，原生入口返回 404，渠道已 fail-closed 禁用；文本令牌仍按用户定义保留这两个 allowlist 项，不冒充健康。GPT 文本内置最小测试约 2–3 秒。图片通用测试按钮会真实生成并计费：GPT Image 2 三次约 36–44 秒；Gemini 图片被错误送往 Chat 后约 62–74 秒失败，因此该按钮不能作为图片/视频健康检查。Preview 图片型号只可作 shadow 候选，不替代 ADR-020 中尚未出现于目录的稳定目标名。视频渠道只包含 `seedance-2-5s/10s/15s`、`grok-video-1.0/1.5`；固定 new-api adaptor 与 OpenOx 创建路径的差异仍由 Compose 内网 `openox-video-compat` 精确桥接，查询与目录透明透传，不开放宿主端口、不记录访问日志或密钥。尚未执行 task-shaped 图片/视频接口验证，也未确认质量、实际成本或完整响应兼容；这不是运行时消费者、模型晋级、路由切换或生产部署。
- **非运行时候选与退役治理（ADR-021/022）**：`site-builder-model-candidate-baseline/2026-08-04-v2` 是当前机器可读候选唯一真值，[人类可读基线](../site-builder/model-candidate-baseline.md) 由它生成并受 `docs:verify` 防漂移。它与 execution policy `site-builder-model-policy/v3` 分离，只登记 registry-only `targetCandidate`，不让候选自动路由；`runnable` 仅表示可进入后续 probe/评测，不表示 task 可用。Gemini 文本仍 `deferred`，Gemini 图片仍 `preview`，视频仍 `deferred`。MiniMax/Doubao 保留为 `legacy-only` 历史 provenance，已从 active route、可执行 rollback、runtime token、attestation、comparator 与价格覆盖移除；完整 rollback 矩阵见[零费用治理卡](../evidence/site-builder/m1-g-text-route-retirement-decision-card.md)。
- **QA/SEO 非权威消费者（2026-07-30 当前交付分支，零模型调用）**：P4 确定性 DesignEvaluation v2 后新增私有 `QualityNarrativeSetV1`；`qa_summarize` 只接 contract/a11y/Lighthouse/genericness/网络素材/视觉完整性等既有 finding，`seo_review` 只接八类 SEO finding 与对应 `seo_report` 的有界检查投影。模型输出只能重排输入 finding ID 并选择服务器冻结的 group/explanation ID，不能新增事实、改变 severity/pass/repair/score 或输出自由文本。空 finding 不进入 executor；付费门拒绝、模型失败和输出无效均写明示 deterministic summary；既有 RESERVED/UNKNOWN/unknown-cost spend 在传入 executor 前阻断，unknown settlement 停止后续付费调用且 lease 清理失败不得覆盖原始错误；取消等待结算后直接终止，不写 fallback checkpoint。证据按 candidate/artifact/round create-only 私有保存，ACK-loss 重试先验 identity/content/hash 后复用；若私有 sidecar 校验或存储失败则只省略叙事，不推翻 DesignEvaluation v2、P4 verdict、repair、ReleaseManifest 或 P5 发布门。该 PR 未改 active route/API/数据库/new-api，真实模型调用 0、费用 $0.00；边界见[消费者决策卡](../evidence/site-builder/m1-g-quality-narrative-consumers-decision-card.md)。
- **代码级候选评测 harness（无新增真实 evidence）**：`site-builder-model-evaluation-harness/2026-08-01-v18` 的机器派生 task/protocol/envelope、canonical suite、闭合结果、排序、预算和 provenance 合同只认[生成评测基线](../site-builder/model-evaluation-harness.md)。BrandProfile 既有 suite 保持；`design_spec` v15 suite 固定六 Family × sparse/rich 的 12 个合成 fixture、每例 2 次、3 个 runnable 候选及一次 GPT-5.5 capability probe，并以 24 个零调用 deterministic catalog-selection case 作为 comparator。copy 有 2 个 Claim/slot fixture；assemble 与 assembly_fix 各有 M1-e-B 12 个 sparse/rich fixture；QA/SEO 各有 2 个 quality-narrative fixture，所有新增 suite 均为 2 repeats、legacy comparator allowlist 为空。当前改动只用 fake wire/fetch/settlement，未读 `.env`、未调用真实模型或媒体、未产生费用或新 evidence、未改 new-api token/channel、消费者、active route、公共 API、DB、P4、Temporal 或 Release。
- **零费用 evidence 准备（无真实 dispatch）**：`site-builder-model-evaluation-evidence-prep/2026-07-29-v1` 从上述 harness/cost-safety 机器合同生成 fixed-commit、create-only 的 BrandProfile 清单：1 个 capability probe + 36 个 target + 24 个 legacy comparator = 61 executions，每次最多一次 schema repair，故最多 122 wire calls。61/122/2440¢ 仅是待验证规划上界；实际费用卡必须从 fixed commit 已跟踪的脱敏 safe-snapshot envelope 复核专用有限额度凭据的不可逆 fingerprint、精确 scope、余额采样时间、spend authorization/ledger、真实冻结价格与计费单位，source bundle 也只认该提交内 Git 跟踪源码。准备 runner 不导入 client、不读 `.env`，输出即使为 `READY_FOR_PRODUCT_DECISION` 也固定 `dispatchAuthorization=NOT_AUTHORIZED`；本阶段没有真实 attestation/费用卡文件、模型请求、费用或 evidence，其他六个 task 与全部媒体仍在 client 前阻断。
- **MODEL-1 BrandProfile fast-follow 与代码级晋级**：6 个无个人数据合成 fixture（3 行业 × sparse/rich，含 EU 目标市场）均经生产 AiTask/证据门和真实 new-api 各跑 2 次。final-code 候选报告 SHA-256=`76f30d38dc958e777b036a29f430963d185399b761e7de5d63f7189b303bad60` 为 24/24 accepted：Terra/Responses 与 Sonnet/Messages 各 12/12、0 hard failure，且每次 requested/reported/resolved 精确一致。完整 legacy DeepSeek Pro→GLM 路由基线 SHA-256=`3aa408b68978779b4a81f3696f68c761adca453e5fafa9d513bf128d41b2d69b` 为 12/12；两成员 probe 通过，正文均由 DeepSeek Pro 完成、fallback 0 次。两报告 source bundle 起止 hash 相同。`20260718-v2` attempt 12 的 24 calls/23 accepted 与 `v3`–`v19` 失败/诊断证据原样保留，不能冒充成功或覆盖。按冻结价格快照，Terra/DeepSeek 单位 accepted artifact 成本为 $0.0025118125/$0.0053631875，Terra 约低 53.17%；三路本轮质量均过门，故成本与已批准失败门支持 Terra 主、Sonnet 回退，DeepSeek→GLM 保持 rollback。该结果只支持 BrandProfile 合成文本子集和代码路由，不等于生产部署、真实租户流量或 de-DE/视觉评测；R4-B-min 只消费其冻结 provenance/usage 做账，未重跑或改写这些证据。
- **已合并（当前 main）**：#121 已把 intake **行为**改为无条件建站/触发 demo；#123 已修 ADR-017 禁虚构身份；#124 已完成 businessEmail 不进 KB/Prompt、LLM 真取消超时、异步失败保留站点并支持原地重试；#125 完成项目真值收口。
- **R0 已闭环（#126）**：intake 已声明并持久处理 `Idempotency-Key`，同键同请求重放首次 Site/Build、同键异请求稳定返回 `409 IDEMPOTENCY_KEY_REUSED`；成功响应固定为 `{siteId,buildId,status:"generating_demo"}`，不再暴露 `mode`；Temporal ACK-loss/AlreadyStarted 收敛、稳定 400/409/502 错误信封与 code-first OpenAPI 已有测试和真 PostgreSQL/Temporal 验证。
- **R2-A1/A4 Asset 状态机已闭环（2026-07-17）**：`pending_upload/failed_retryable/expired committing → committing` 由 attempt+UUID token+lease CAS 认领，完成/失败回写全部 fenced；staging cleanup 保持代码固定的 15 分钟在途宽限、首删+HEAD、durable 5 分钟与二次删除+HEAD。canonical 删除的引用/回收安全由 MF0-B 接管（见下）。
- **R2-A2 KB 已闭环（2026-07-17）**：canonical doc 的 `queued(due)/expired processing → processing` 复用 Asset attempt+UUID token+lease，所有续租、失败与完成回写 fenced；`KbDocument.assetId` 历史重复按健康度/向量版本/更新时间确定性收敛后加 nullable unique，Site→Asset/KbDocument 以 `id+workspace`、document→Asset 以 `id+workspace+site`、chunk→document 以 `id+workspace` 复合 FK 守 provenance。历史 Site/workspace 不一致的子行无法安全跨租户改写，迁移会中止并要求显式 quarantine/audit，绝不硬删 canonical Asset 或绕过 R2-A4/MF-0 cleanup 账本；当前开发库预检此类行数为 0。文档/chunks 与 Asset ready 同一事务提交，旧 worker 接管后不能 zombie write；MinIO/Docling/BGE 以 typed code 分流，瞬时错误回 `queued+retryAt`，只有结构化证据确认的真损坏文档才 `failed_terminal`。单素材 workflow 不再无界扫站，5 分钟 recovery Schedule、有界扫描/结构化告警和人工 redrive 已落；R2-A4 已补齐跨系统 cleanup 与公共错误合同。开发环境真 PostgreSQL/app_user RLS/MinIO/Docling/BGE、34→37 脏数据升级和空库 37 migrations 已验证；不代表生产部署。
- **R2-A3 Profile 已闭环（2026-07-17）**：五组 Profile 采用严格组内 schema、数量/字符串/关系/URL/同站 Asset 引用与 64 KiB 总量门；URL 保存只规范化、不触发网络。Profile 有独立 UUID token，GET/PATCH 同回 `versionId`、强 ETag 与私有缓存策略；PATCH 缺判据 428，body stale 409、header stale 412，数据库 `updateMany(id+profileVersionId)` CAS 禁止 last-write-wins。旧 M0 脏 JSON 的 GET fail-closed 为 `409 PROFILE_MIGRATION_REQUIRED`（只给 path/group/action 与 ETag，不回显值、不自动映射）；Brand Prompt/证据语料入口递归遮蔽 ASCII/SMTPUTF8/IDN 邮箱与电话。开发库 38 条 migration 账本逐项与仓库一致，已恢复 #126 首次部署 migration 的原始字节；真 app_user/FORCE RLS、双连接同 base 恰一胜、失败方 re-GET/retry 保留两组、跨租户 404，以及空库 38 migration/schema diff=0 均已验证。当前开发库 `site` 为 0 行、无存量 Profile；这里只代表 Ubuntu 开发环境，不代表生产部署。
- **MF0-A/B 媒体地基已完成（2026-07-17）**：MF0-A 新增 `AssetVariant`、复合 provenance、RLS/FORCE RLS、单输出 recipe、严格 key/checksum/status 与 `derivedKeys` 兼容投影。MF0-B 新增 Profile 三面 + 当前 `activeVersionId` SiteSpec 的 fail-closed 扫描、`409 ASSET_IN_USE` 稳定 usages、Profile/指针/Variant/DELETE 共享 Asset 锁与 Variant trigger；canonical producer 由 key-level advisory lock 关闭同 hash TOCTOU，schema v2 冻结≤128个无环 Variant，Temporal 两轮 Delete+HEAD 后叶→根删行并 durable settle，settled 旧事件重放不碰 replacement。历史 v1 parked 默认 dry-run 分类并生成带 causation 的 v2 successor；迁移前无账本 tombstone 用 `cleanupLegacyUnbound` 隔离，新写不能伪造，lifecycle CHECK 已 validated。Ubuntu 开发库 46 migrations、schema diff=0、真 PG 并发/对账与真 MinIO+Temporal cleanup/replay 均绿；不代表生产部署。
- **M1-c 纯 Sharp 图片管线已完成（2026-07-17 当前交付分支）**：API 直接精确锁定 Sharp 0.35.0，`pipelineVersion` 同时固定 libvips；≤20 MiB/40 MP/4 channels/单页严格解码，自动方向、sRGB、默认剥 EXIF/GPS/XMP，模糊/曝光/噪点只产生版本化 warning。按 kind→role 与显式 focal point 做不放大裁切，输出 320/640/960/1440/1920 中源图可承受的 AVIF+WebP+JPEG/PNG fallback；logo/cert 三编码均无损。inspection 与编码全部在 `concurrency=1/cache=false` 且可超时终止的子进程内；父进程严格校验协议、路径、regular/non-symlink、MIME magic/hash/bytes，进程级门默认并发 1，单输出/整批/result 有硬上限；Ubuntu 编译子进程另有 `prlimit`，但这仍是开发 containment，**不是生产容器/cgroup/独立 UID/禁网沙箱**。writer 在首次对象写前为完整 recipe set 持久预占带 producer token/lease 的 `processing` 行；过期 producer 只能写带一日 lifecycle tag 的 token 隔离 `variant-attempts` key，重新锁定 Asset、校验 token 并取得 canonical key fence 后才可在 15 秒有界事务窗口内 promote，canonical copy 显式剥离 TTL tag。ready 账本缺对象/身份不符时 fail-closed；attempt 在重试前以 8 路有界 IO 删除并压缩，settled cleanup 重放只重删冻结 attempt，S3 调用接 Temporal cancellation+110 秒 deadline。attempt key 写入 durable metadata/cleanup plan；ready set 与 manifest 仍原子收口，失败 CAS 只作用于本 token；单 Asset 最多 120 个 Variant 行，新 attempt-aware cleanup 总对象≤128（历史无 attempt payload 兼容 128 Variant+canonical）。bucket lifecycle 在生产默认 validate-only、缺失阻止启动，仅单一部署 owner 可管理。共享 recipe 保留 MF0-A 旧合同，并以显式 V2 承载完整 operations identity。refurbish P2 首个 Activity 一次物化≤512 个排序 Asset ID 的不可变 workset，后续按两张/Activity 处理；批间 INSERT 不混入、既有成员 UPDATE 不漏项，超限在启动 Sharp 前显式降级。旧 cursor/finalize payload/replay 保持兼容，逐图降级且取消在 commit point 前穿透；`hasPerson` 只保留已有真值，`aiEdited=false`。真 Ubuntu 开发 PG/app_user RLS/MinIO 验证 30 项断言：同 key 双 producer、30 变体、零编码重放、PUT 响应丢失、写前 owner、中断只落 attempt key、接管、冻结 ID workset、容量门、坏图隔离、RLS 与残留清零；不代表生产部署，生产还须独立验 lifecycle、容器隔离、权限、容量、备份与监控。MediaJob、AssetUsage、rembg、生成式图片、视频、公开 process/select API 与 Renderer `<picture>` 均未加入。
- **R3-A + R3-B1/B2 已完成（2026-07-17 当前交付分支）**：R3-A 为 `SiteBuildRun` 落复合 provenance FK、合法状态 CHECK、每站 active 单飞与 nullable Temporal identity；B1 收口严格请求合同、请求指纹幂等与 `workflowId + firstExecutionRunId` ACK。B2 新增 FORCE RLS 的 `SiteBuildStep` attempt 真值，phase/progress 只前进，旧 attempt 迟到不可覆盖，成功/失败/取消均终态化；`scope=page|section`/`options.pages` 冻结请求时 active `baseVersionId`，只合并目标 page/block 与对应文案键，发布以 active pointer CAS 防并发人工编辑被覆盖。新执行先写 run-scoped staging/隐藏 immutable 本地版本，数据库提交后才以单次原子 symlink rename 切 `.active/<slug>`；CAS/事务失败保留旧预览，提交后崩溃由 Temporal retry 从 durable artifactKey 重建 pointer。这仍不等于 R1-min 的生产对象存储 Release、跨节点恢复与回收。目标缺失/重复/脏 spec fail-closed，stylePreset 不得逃逸局部 scope。B2 交付时非 `en` 明确 422；M1-d 现已接入 `en/de-DE` 生成与 Renderer 消费。Ubuntu 开发环境完成真 PostgreSQL app_user/FORCE RLS、隔离真 Temporal 成功/取消验证，夹具/namespace/worker 已清理，**不代表生产部署**。
- **R4-A1 Evidence 2.0 基础已完成（2026-07-17 当前交付分支）**：新增共享 `EvidenceRefV2`、租户范围内不可变 `SiteEvidenceSourceSnapshot` 与 `BrandProfileEvidenceRef`；模型调用前先对 intake/KB/storefront/research 做 PII 清洗、规范化、完整 SHA-256 和有界冻结，KB provenance 精确到 document/asset/chunk/hash。intake website URL 也先去 credentials/fragment/敏感 query 与 path/query PII；SearXNG 上游 title/snippet/path/query/fragment 不进入冻结语料或 provenance，只按站主公司名 + external origin 生成最小 `research_hint`，持久化边界再次重建该 hint，并跳过无法证明已最小化的历史 `KbDocument.source=web_research` 正文，防第三方具名个人落入不可变账本。新 BrandProfile 写入固定 `evidenceSchemaVersion=2`，每条事实必须由服务端绑定已冻结 source/type/hash 与 8–512 code point 精确 quote/selector；旧 v1 行继续可读且不伪造回填。两张新表 FORCE RLS、应用角色仅 `SELECT/INSERT`，复合 FK 锁定 workspace/site/profile/source/hash，UPDATE trigger 拒绝改写；删除仍由 owner 沿 Site retention cascade。迁移为 additive/forward-only，回退须先部署 v1 reader/writer，并只在证明无 v2 行后另作 forward migration。无公开 API/OpenAPI 变化。A1 当时 head 真机 verifier 已完整通过：真 PostgreSQL 两账本的 ENABLE+FORCE RLS、`app_user` 仅 SELECT/INSERT、A/B/unset 隔离、跨租户 FK 拒绝与 UPDATE trigger 全绿；真实 SearXNG+Crawl4AI 返回 storefront + 5 个 origin-only web hint，逐项验证无原始 title/snippet provenance，BGE-M3 KB 摄入完成；DeepSeek Pro 两轮 Activity 分别追加 BrandProfile v2/v3，全部事实的关系 ref、quote selector 与 source hash 均通过，重复 intake/KB snapshot 正确去重。第二轮一次研究降级被显式标记后安全继续；verifier 自证隔离夹具清零。此前 DeepSeek 150 秒超时与 GLM `finish_reason=length` 仍证明模型通道会波动，方舟 11/11 连通也不等于 task-shaped 稳定；当前 head 的 Terra 复验见下一条。上述只代表 Ubuntu 开发环境，不代表生产部署。**A1 自身只证明引用完整性**；其后的事实真值与内部发布资格由 R4-A2 收口，整体重试复用与成本真值已由 R4-B-min 收口。
- **R4-A2 Claim/Evidence truth bridge 已完成（2026-07-19 当前交付分支）**：BrandProfile 出口以公共企业事实 allowlist、PII/未消歧案例门和 value/quote 关键值对齐 fail-closed，`research_hint` 永不投影。新 intake 原子绑定 CompanyProfile→Site，旧未关联 Site 不猜身份。通过门的 fact 只接受 `^[a-z][a-z0-9]*(?:_[a-z0-9]+)*$`，模型 schema、确定性 gate、投影哈希、EvidenceRef 与 Claim 数据库约束共用同一语义；混合大小写、空格或连字符输入直接拒绝，不静默改写。稳定 origin key 复用共享 Claim/Evidence，不可变 FORCE RLS bridge 与 exact trigger 锁定公司、fact、snapshot/hash/quote/selector/asset；机器 Claim 只进 `NEEDS_REVIEW`。origin Claim 审批先锁 Claim 并预检 exact bridge，数据库固定 search_path 的 security-definer trigger 再锁 surviving bridge，因此 Site/BrandProfile 已删后的孤立 Claim 在 API 与 owner 直写均不能 APPROVED；manual/legacy 的 `originKey=null/factKey=null` 边界保留。Workspace 若仍有 Site 会拒绝物理删除，不能再绕过对象清理/outbox 级联；cert Asset 删除扫描走 `(cert_asset_id,site_id,brand_profile_id,fact_index)` partial covering index。认证仍须同 workspace/site 的 live ready cert Asset，审批/撤销/到期/冲突裁决继续 CAS+outbox 原子收口。`20260718170000`–`20260718188000` 共 19 个 forward migration 已在 Ubuntu `global_dev` 通过旧数据预检；真 PostgreSQL verifier 覆盖并发单赢家、strict factKey、Workspace guard、孤立审批、EXPLAIN、RLS/immutability/FK 并清零夹具。A2 没有新增 endpoint、SiteSpec/CopyBundle/正式 `PublishableClaimSnapshot`，只增加 nullable `ClaimDto.factKey` 与 Asset usage `claim_evidence` 等 additive 合同；不代表生产部署。
- **R4-B-min 付费执行账本已完成（2026-07-19 当前交付分支）**：每个 BuildRun 原子创建硬预算，`SiteBuildBudget`、`SiteBuildTaskAttempt` 与 `SiteBuildSpend` 以数据库函数执行并发 reserve/settle/reconcile；稳定 operation key、attempt lease/fence 与冻结 input/output 令 BrandProfile 重放复用成功产物，接管只在 lease 过期后发生。模型与 ToolBroker 共用同一 durable ledger；provider 报告、冻结 MODEL-1 token 价格、legacy estimate、unknown 与 not-incurred 分开保存，预算保守扣款不伪装成真实成本。结算 ACK 不明会标记 unknown 并停止 fallback；预算耗尽、run 非运行态或取消立即关闭 `paidCallsEnabled`，终态 reconciliation 关闭开关并写闭合的 `site-builder-cost-summary/v1`。OpenAPI 已冻结该版本合同。Ubuntu 开发环境的真 PostgreSQL verifier 已覆盖 FORCE RLS、双连接并发 reserve、ACK-unknown/replay/settle、预算与取消 kill switch 和终态 summary；不代表生产部署，也没有重跑或改写 MODEL-1 证据。
- **M1-d 多语种事实受限文案已完成（2026-07-19 当前交付分支）**：正式不可变 `PublishableClaimSnapshot v1` 只纳入 approved/current/audited 且与 Site 精确桥接的 Claim/Evidence，激活前锁 Claim 重验撤销/到期/版本/bridge/cert；空 snapshot 走无模型中性文案。`CopyBundle v1` 按 SiteVersion/locale 绑定 snapshot、R4-B task attempt、input hash/digest，grapheme 预算、restricted rich text、Claim refs 与 outbound domain 均 fail-closed；模型只建议逐 slot refs，代码过滤未知/重复/超预算引用，带 ref 时逐字重建 Claim statement、无 ref 时强制中性文本。完整 bundle 校验后才完成 task attempt，不能永久重放无效结果。生成 locale 冻结 `en + de-DE`，默认失败阻断、可选失败省略+degraded；权威局部构建必须重建完整 active locale 集，否则 API/assembly 双门拒绝，不能丢既有路径；`ar` 只做 RTL renderer smoke。SiteSpec 新 `copyBundleSet` 权威、旧 `copyBundles` 一周期 dual-read 且新集合缺 locale 不偷读英文。renderer 已有 locale routes、精确 lang/dir、自托管 Noto 字体与产物外链扫描；inquiry 仅合同、表单仍 disabled，接收/投递属 M2。两项 migration 已在空库及 `global_dev` 应用，真 RLS verifier 与三语 Astro build 通过；不代表生产部署，copy route 未做 MODEL-1 晋级。详见 [实施记录](../implementation-records/site-builder-m1d-copy.md)。
- **M1-e-B 受控组装已完成（2026-07-24 当前交付分支）**：Catalog `m1-e-b/1.0.0` 恰含 `precision-industrial`、`technical-catalog`、`oem-capability`、`scientific-trust`、`natural-origin`、`premium-innovation` 六个 approved Family，各具 2 首页、2 内页、2 Hero、至少 2 preset、固定 DemoVisualPack 与 sparse/rich Golden。DesignBrief 只从 approved 候选确定性排序，模型只能在前三候选中建议 ID，非法输出/provider 普通失败回到首选，取消/预算/未知结算不绕过。受控组装的服务端 adapter 是 props 唯一生产者，模型不能写组件、variant、正文、CSS/HTML/Astro；三轮修复后只允许同 Family safe Blueprint。SiteSpec 1.1 冻结组件库/Renderer/Family/archetype/locale dir，tenant/catalog asset 经 hash、RLS、路径与 symlink 门物化到一次性 overlay；ReleaseManifest v2 覆盖完整 DesignBrief/spec/files/version digest，局部构建必须复用 ready v2 base 的 Brief/seed/copy/assets，v1 base 稳定拒绝。Temporal 新历史按 `design_spec → copy → assemble_build` 运行，旧 history 仍走 patch 旧分支；进度数据库 CHECK 与 Release v2 envelope 以同一增量 migration 扩展。Ubuntu 开发环境真 Temporal/PostgreSQL RLS/new-api/Astro/MinIO/SiteRelease v2 链通过；new-api 当时真实返回 402 余额不足，流程只证明已调用并按既有开发回退安全完成，**不证明模型成功或生产部署**。
- **M1-f DesignEvaluation / P4 quality loop 已完成（2026-07-24 当前交付分支）**：新 refurbish history 在 P3 只创建一个 fenced `building` candidate，P4 对完整站点执行确定性合同/SEO/a11y/genericness、375/768/1440 三断点截图与 Lighthouse；只有通过后才将最终质量证据复制到 Release 私有区、写 `ReleaseManifest v3` 并用 active pointer CAS 激活。round 0 初评后最多三次修复；模型只能从服务端按当前 spec/brief digest 生成的同 Family approved Blueprint/variant/item/Asset `optionId` 中选择，实际变更仍由服务端应用并重跑全部 validator。失败、取消、预算关闭、materialize 异常或 pointer 冲突均保留旧 Release；pointer 冲突产生的 ready/inactive Release 保留供幂等重试或 GC。旧 history 通过 `site-builder-m1f-quality-loop-v1` patch 保持原命令序列；v1/v2/v3 reader 兼容，v2/v3 partial base 均评测完整站点。Gemini 原生图像接口及 Terra/Responses、Sol/Responses、Sonnet/Messages 均已连通，但复核发现现有确定性 Golden 不是独立审美 Gold，旧四模型结果只能作诊断，不能判定模型胜负或晋级。运行时只允许确定性全绿后以 `passed_deterministic_aesthetic_unavailable` 激活，并记录实际型号、协议和错误，绝不称为模型成功。真 Temporal SDK 1.20.3 已回放 M1-e-B 完成与失败历史，并注入 round 0/2/3、Activity ACK-loss、真实 worker 重启、取消、materialize retry 与 pointer 冲突；每个场景保持一个 building SiteVersion、最多一个 Release、失败时旧 pointer 不变，三项上游付费 Activity 均恰执行一次。上述仍只代表 Ubuntu 开发环境，不代表生产部署。
- **双模型盲评视觉校准 harness 已实现、尚未实跑（2026-07-25 当前交付分支）**：独立离线模块把六 Family 的 `deterministic_render_baseline` 与确定性劣化图组成 6 对，每对只传两张同断点图并做 3 次固定左右顺序；模型 closed output 仅允许 left/right/tie 与最多 4 条冻结 ruleCode/severity/imageNumber。四候选各冻结 1 probe+18 calls、120 秒、800 tokens；质量优先地保留原始 PNG。campaign launcher 在任何调用前只核验四个型号/协议、固定源码和截图哈希，随后按 Gemini→Sonnet→Terra→Sol 串行、隔离执行；首轮不以中转账本或预估金额阻断调用，而是逐次记录实际 token/成本，并在结束后为下一轮输出原图成本预测。服务端纯函数执行 18/18 格式/provenance、≥17/18 已知问题、≥5/6 Family 稳定门，并只在不同 upstream model family 的两个单模型通过者之间计算 ≥17/18 共同代码+位置命中。当前没有真实模型调用或新证据，旧 v3/v4 诊断未改；P4、DesignEvaluation v2、ReleaseManifest、Temporal 与运行时路由均未变化，审美仍为 unavailable。最高可能结论仅为可进入独立审美 Gold 校准，不是模型晋级或路由切换。
- **SiteBuildStep schema drift 已独立修复**：`site_build_step.id` / `updated_at` 的 Prisma 与数据库默认表达式已在 M1-f 前置 PR 单独对齐，空库全迁移、现库 migration status 与 migration-to-schema diff 均为零；未借质量循环改变既有行语义。
- **DOC-12 状态**：#119/#120 已把主要内容分发进 00–14 并登记 ADR-013~019；2026-07-17 新增 ADR-020 记录当时的模型组合决策，2026-07-27 ADR-021 以独立 candidate baseline 防止新网关目录反向改写历史。v3.1/v3.2 已降为 dated proposal。此前把“内容分发”写成“全项目真值已收口”过早；2026-07-16 truth-sync 已纠正权威层与接入文档，未把旧 Word/研究稿升级为权威。
- **R1-min 已完成（2026-07-20 当前交付分支）**：新增 RLS `SiteRelease` 不可变账本与 manifest/digest、producer/build/GC fencing；Demo/refurbish 均先写 producer-isolated S3/MinIO Release，再以 `Site.activeVersionId` 单一指针事务晋级。跨节点 activity retry/ACK-loss 复用同一 run/version/release，取消与并发 active CAS 保留旧 Release；公开 `/preview/{slug}` 只经窄 SECURITY DEFINER resolver 读取 active READY Release 并逐对象验 digest。unknown component/unsupported SiteSpec 在 renderer 与上传前 fail-closed。定时遗留 candidate/retention 对账已接线，GC 默认关闭，显式开启后仍保留 active、30 天内 READY 与每站最新两个回滚点；这不代表生产部署。
- **DI-0 已完成并由 M1-e-B/M1-f 消费（#164，2026-07-22→24）**：`@global/contracts` 运行时 fail-closed 校验 `DesignSourceManifest`、`DesignObservation`、`DesignRule`、`DesignDNA`、`TemplateFamily`、`DesignBrief`、DesignEvaluation v1/v2 与静态 `DesignCatalog`；来源策略、至少五个独立贡献组、目录/Family digest、approved-family-only resolver 和递归冻结均有自动化负例。DI-0 交付时 Catalog 故意为空；M1-e-B 已落六个 approved Family 与真实 assembly/Renderer 消费，M1-f 已生产 closed-shape DesignEvaluation v2 并禁止任意 patch/props/CSS/HTML。
- **当前关键路径**：R1-min、DI-0 与 **M1-e-A/B、M1-f、M1-g 均已完成阶段收口**。M1-g 建立统一 verifier，完成六 Family 的 12 个 sparse/rich SiteSpec、36 张三断点冻结截图、空 Claim 事实零虚构和 10 站跨站 genericness；无模型真链已通过 PostgreSQL/FORCE RLS、MinIO、en/de-DE/Release v1、new-api `/models`、Astro 与 Temporal 六场景。付费 campaign 只保留为诊断，不能冒充 canonical promotion evidence；因此六任务均不晋级，现役安全回退/路由保持。历史 runtime/恢复决策卡继续只供审计，不再是 M1 收口阻塞。30+ 系统集、MODEL-2、独立审美 Gold、生产部署与 M2-PUBLISH 继续登记未完成；`template-distillation` 未被采纳。
- **抓取出口 as-built**：Ubuntu mihomo fake-IP 兼容不再使用 broad allow-internal。只有系统解析结果全部落入 `198.18.0.0/15` 时才走固定 Cloudflare DoH；API 连接钉扎并逐跳重验 redirect，Crawl4AI 保留 global-unicast seed guard 与浏览器 pinning proxy。公网 `/md`/`/crawl` 正例和 private/loopback/metadata/IPv4-mapped/redirect-to-metadata 负例均已通过；`:11235` 仍只绑 loopback。
- **已拍板、尚按消费者推进**：55 型封闭组件库（ADR-015）· 模型档四态路由（ADR-016）· MF-0 薄媒体地基（ADR-018）· Readdy 净室参考（ADR-019）· 不可变 Release（ADR-013）· 质量优先历史组合（ADR-020）· 新中转站候选重基线（ADR-021）。当前候选只认 `site-builder-model-candidate-baseline/2026-08-04-v2`；只有 BrandProfile 经独立证据成为代码级 `promotedRoute`，其余仍逐 task 服从 capability probe、Golden Set、成本/质量、失败门与回滚门，真实外部流量/高风险部署前再走 MODEL-2。ACCEPTED 不等于已实现或已部署。
- **协同事实源**：[00-decisions-and-coordination.md](../site-builder/00-decisions-and-coordination.md)。承重决策只认 [ADR 注册表](../adr/registry.md)，施工序看 [09](../site-builder/09-m1-implementation-design.md)。

---

## 真实具备（获客历史快照·截至 2026-07-13；9 组，均有当时的真实数据验证记录）

企业理解（Claim/Evidence/冲突/人审）· ICP（规则/回测/查询计划/CPV/FDA 映射）· 发现（public_web/wikidata/OSM/名录/展会/TED/openFDA）· 富集（GLEIF/Wikidata/数字足迹/结构化收割）· 采集监控（4 表+增量 diff）· 买家信号（**一等 Signal**：`source_signal` 平台表两层模型 + `signal_ingest` ingest-once 账本（指纹×6h 窗跨 workspace 只拉一次）+ 状态机（EXPIRED/REVOKED 剔除、撤即脱敏）+ 可复算投影（recompute 同过滤面）；web_watch/TED 招标/510k 清关/决策人/smtp_self）· 资格（六维评分 + **demand_proof 观测维**（需求证据类事件驱动，不进总分待乘法门 backtest）/Reachability 硬门/四队列/人工裁决）· **事件交付**（LeadQualified 快照 v1 契约（demand_proof 已填真值，rule=additive-6dim-v2）/`outbox_delivery` 账本/`GET /events` 拉取+ACK/可选 HMAC webhook）· 平台地基（JWKS 座/RLS/Outbox/Temporal 4 Schedule/模型网关/API 门户 + **统一信封契约**：code-first openapi.json 单一真值、CI 契约三道门 + **ToolBroker 唯一执行闸门**：13 个 L0 工具收编全部主链出网、source_policy fail-closed 分层、预算 reserve-then-settle 真拦截、ExecutionContext 贯穿真租户归账）。

## as-built 缺口（8 项，已核验；#1–#5 已修，#7 大部完成；剩 #6/#7 follow-up/#8）

见 [../architecture/current.md](../architecture/current.md) §8。**#1 Fit 聚合根已修（PR #43，收口①完成）**；**#2 Broker 非唯一闸门已修（PR #51，收口②完成）**——13 个 L0 工具收编 22 处直连出网（发现/富集/intent/采集/理解五链全经 `broker.invoke`，例外四类登记），source_policy `required|advisory|none` 分层 fail-closed + 用途门按调用 purpose 判，预算真开账（run/sweep 开关账 + LLM 网关 reserve-then-settle 按 token 折算 + 截断显性化 run 转 PARTIAL），伪 workspace 清零、AI trace 真库实证写入成功；**#3 Outbox 假发布已修（PR #46，收口③完成）**——LeadQualified 现以快照 v1 契约经交付账本真实交付，未注册事件 parked 不再假发布；**#4 OpenAPI 双源已修（PR #48，收口④完成）**——统一信封定稿（`{data}`/`{data,page:{next_cursor,has_more}}`/`{error}`），旧 YAML 删除、契约唯一真值=code-first `openapi.json`，CI contracts job（drift+spectral+oasdiff breaking，label 放行）拦截破坏性变更；**#5 Intent 非一等事实已修（PR #56，收口⑤完成）**——`source_signal` 一等信号表（零个人数据、双时间轴、状态机+TTL）+ `signal_ingest` ingest-once 账本（同源同参同 6h 窗跨 workspace 只拉一次），TED/openFDA 投影反转为只读平台表、sweep 四段化，`attributes.intent` 降为可复算投影（recompute surfaces 同过滤面 + mergeIntent epoch 归一），快照 demand_proof 真值填充（v1 槽位零破坏，rule→additive-6dim-v2）。

**收口⑤记档项**（不阻塞）：ingest PENDING 抢锁根治（TOCTOU 双拉窗口，ERROR 覆盖 OK 已修）· TED CPV 前缀列+GIN 下推（现扫描窗打满有显性告警）· 投影 maxCompanies 游标化（现 subjectsTruncated 可观测，与缺口#8 同类）· license 值 SPDX 统一（随收口⑥ 权利词表）· SUSPENDED=停采不停用两级撤停语义已文档化（存量处置=revokeByProvider）。**#7 存储合规主体已修（收口⑥，PR-A #60 + 后续删除编排）**——`DataRightsService` 7 动作确定性引擎 + jurisdiction_policy(含 PIPL) + policy_decision_log/lia_record/article14_notice + PII 列级加密 + DB 角色拆分；`deletionWorkflow`(GDPR Art.17 冻结→擦除→重评分→回执) + `deletion_request`(状态机 RLS) + `deletion_receipt`(append-only + FK RESTRICT 防级联删) + `POST/GET /deletion-requests` + 事务性 outbox 起编排 + DeletionCompleted 事件。历史 DSR 全链演练记录保留在 changelog/PR；当前测试状态以实际 CI 为准。

仍需诚实登记：**#6** 身份图最小版（merge/split/回放）未落；**#7 follow-up** 的 consent/retention sweep/roles→scopes 仍待；**#8** 跨-sweep 游标公平性仍待。获客恢复商业试点前，还须满足 [release-plan](../roadmap/release-plan.md) 的真实企业 E2E/UAT 与“输出被下一能力真实消费”封版条件。

## 与 PRD v3.0 的登记偏差（8 条，不再静默）

new-api 替代 LiteLLM · 全 TS 无 Python worker · 不用 BullMQ · 自建 ai_trace 替代 Langfuse · OPA 未上（PolicyPort 过渡）· 免费源优先替代付费瀑布 · AiToEarn/触达不在本仓 · recommended 队列门（总分≥0.55 且 Reachability>0）为 PRD 缺位的实现口径。

## 待拍板（3 项 + 1 已落地）

1. ~~制裁名单筛查立项~~ **✅ 已落地（#104，2026-07-14）**——OFAC(SDN+Consolidated)/EU FSF 公司实体筛查作 qualify 第五门，命中→`sanctions_hold` + decide 硬拦，默认 DISABLED（翻 ENABLED 免 LIA=不物化 PII）。ADR-010 已更新记档。
2. **DLP 提单前提核实**——「海关提单」免费方案的前提（DLP FOIA 数据集可再分发）疑似 2023-04 已被拒。需人工上 data-liberation-project.org 核实（10 分钟），结论记入本文件。R3 排期前完成。
3. **两份 v3.0 Word 最小整改**——修撞号/损坏标题/声明重复章节以 PRD 为准/裁决 M2 口径（QGO vs SAO）。预计 1-2 天；获客开发冻结已提前解除，但 M1 收口前不实施，本项也尚未被选为恢复后的首个任务或分配执行 owner。与交付包「冻结 v3.0」不冲突（只修结构便于迁移引用）。
4. **首发 Job 二选一**——进口商/采购商发现 vs 经销商招募。现有数据源覆盖明显偏向前者（TED/openFDA/提单/web_watch 求供应商页）。建议：**首发「进口商/采购商发现」**，经销商招募 R3 用 Pack 加入。

**需 A/B 会签（用户方向已认）**：PDR-001 对象词典 / PDR-002 业务层级（GrowthInitiative）/ 三接缝契约 / 对象级 RACI。
