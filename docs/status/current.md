# status/current —— 当前真值与待拍板（活文档）

> 2026-07-10 立；**2026-07-17 R1-safety、R2-A1–A4、MF0-A/B、M1-c、R3-A、R3-B1/B2 与 R4-A1 更新**。这是当前状态权威；历史见 [../roadmap/changelog.md](../roadmap/changelog.md)。
> 下方「真实具备/缺口/偏差/待拍板」四节是**获客侧快照，冻结于 2026-07-13 获客暂停时点**。测试数量随提交变化，不在活状态文档固化；是否通过以当前分支实际命令和 CI 结果为准。

## 🟢 当前主线：独立站建设（Site Builder）

获客侧 **2026-07-13 起暂停**（用户指示，明确通知才恢复）。当前唯一开发主线 = 为出海企业一键生成 / 精装修独立站。

- **M0 as-built**：注册引导、`Site`/`SiteVersion`/`BuildRun`、素材上传与 KB、Demo v0 Temporal 工作流、Astro 渲染器（当前注册 10 型组件）、7 个有界 AI Task 路由，以及 **DQ-1 SiteSpec 共享契约（#117）**。`@global/contracts` 的 `SITE_SPEC_VERSION=1.0.0` 是生产/消费两端唯一类型源；运行时 Zod 与 1.1.0 目标字段尚未落地。
- **模型网关当前真值（2026-07-18）**：本机 new-api 的通用令牌 `/v1/models` 返回 **39 个可调用型号**；除原 DeepSeek/方舟路由外，已包含 `gpt-5.6-terra/sol/luna`、`claude-sonnet-5`、`gemini-3.5-flash`、`gpt-image-2`。#140 已合并，自托管 BGE-M3 在 Ubuntu 开发环境通过私有别名 `site-builder-bge-m3-local` + 专用模型受限令牌由应用统一经 new-api 调用；真实 `EmbeddingsClient` 两条输入均返回 1024 维有限向量，令牌 `/v1/models` 只返回该别名。该结论不代表生产部署。MODEL-0 已把 `task-routes.ts` 的当前 primary/fallback 纳入可审计的 ModelProfile/策略快照，**但未因通道扩容切换其生产选择**。当前 7 条已注册 task 中，只有 `brand_profile` 有真实工作流消费者；其余路由不是“已切生产”的证据。`gemini-3.1-flash-image`、`gemini-3-pro-image`、`gemini-omni-flash-preview` 不在当前令牌可调用清单。通道/型号可见只证明 endpoint 能力面，不证明长结构化任务、视觉/编辑输入、固定 schema 或质量门已通过。
- **MODEL-1 评测基座（BrandProfile 文本子集）**：`apps/api/test/fixtures/golden-companies/brand-profile/` 现有 6 个无个人数据的合成 fixture（3 行业 × sparse/rich，含 de-DE），`evaluate-site-builder-brand-profile.mts` 以生产 AiTask/证据门对真实 new-api 候选做手工评测，保存每次 accepted artifact、token 与延迟。它不写库、不改生产路由、不能代替截图/视觉评测、也不证明 Copy/Design/Assemble/QA/SEO 已被评测；new-api 当前不回传可核对的货币成本，故成本硬门仍未满足，任何结果至多为 `evaluatedCandidate` 证据而非晋级。
- **已合并（当前 main）**：#121 已把 intake **行为**改为无条件建站/触发 demo；#123 已修 ADR-017 禁虚构身份；#124 已完成 businessEmail 不进 KB/Prompt、LLM 真取消超时、异步失败保留站点并支持原地重试；#125 完成项目真值收口。
- **R0 已闭环（#126）**：intake 已声明并持久处理 `Idempotency-Key`，同键同请求重放首次 Site/Build、同键异请求稳定返回 `409 IDEMPOTENCY_KEY_REUSED`；成功响应固定为 `{siteId,buildId,status:"generating_demo"}`，不再暴露 `mode`；Temporal ACK-loss/AlreadyStarted 收敛、稳定 400/409/502 错误信封与 code-first OpenAPI 已有测试和真 PostgreSQL/Temporal 验证。
- **R2-A1/A4 Asset 状态机已闭环（2026-07-17）**：`pending_upload/failed_retryable/expired committing → committing` 由 attempt+UUID token+lease CAS 认领，完成/失败回写全部 fenced；staging cleanup 保持代码固定的 15 分钟在途宽限、首删+HEAD、durable 5 分钟与二次删除+HEAD。canonical 删除的引用/回收安全由 MF0-B 接管（见下）。
- **R2-A2 KB 已闭环（2026-07-17）**：canonical doc 的 `queued(due)/expired processing → processing` 复用 Asset attempt+UUID token+lease，所有续租、失败与完成回写 fenced；`KbDocument.assetId` 历史重复按健康度/向量版本/更新时间确定性收敛后加 nullable unique，Site→Asset/KbDocument 以 `id+workspace`、document→Asset 以 `id+workspace+site`、chunk→document 以 `id+workspace` 复合 FK 守 provenance。历史 Site/workspace 不一致的子行无法安全跨租户改写，迁移会中止并要求显式 quarantine/audit，绝不硬删 canonical Asset 或绕过 R2-A4/MF-0 cleanup 账本；当前开发库预检此类行数为 0。文档/chunks 与 Asset ready 同一事务提交，旧 worker 接管后不能 zombie write；MinIO/Docling/BGE 以 typed code 分流，瞬时错误回 `queued+retryAt`，只有结构化证据确认的真损坏文档才 `failed_terminal`。单素材 workflow 不再无界扫站，5 分钟 recovery Schedule、有界扫描/结构化告警和人工 redrive 已落；R2-A4 已补齐跨系统 cleanup 与公共错误合同。开发环境真 PostgreSQL/app_user RLS/MinIO/Docling/BGE、34→37 脏数据升级和空库 37 migrations 已验证；不代表生产部署。
- **R2-A3 Profile 已闭环（2026-07-17）**：五组 Profile 采用严格组内 schema、数量/字符串/关系/URL/同站 Asset 引用与 64 KiB 总量门；URL 保存只规范化、不触发网络。Profile 有独立 UUID token，GET/PATCH 同回 `versionId`、强 ETag 与私有缓存策略；PATCH 缺判据 428，body stale 409、header stale 412，数据库 `updateMany(id+profileVersionId)` CAS 禁止 last-write-wins。旧 M0 脏 JSON 的 GET fail-closed 为 `409 PROFILE_MIGRATION_REQUIRED`（只给 path/group/action 与 ETag，不回显值、不自动映射）；Brand Prompt/证据语料入口递归遮蔽 ASCII/SMTPUTF8/IDN 邮箱与电话。开发库 38 条 migration 账本逐项与仓库一致，已恢复 #126 首次部署 migration 的原始字节；真 app_user/FORCE RLS、双连接同 base 恰一胜、失败方 re-GET/retry 保留两组、跨租户 404，以及空库 38 migration/schema diff=0 均已验证。当前开发库 `site` 为 0 行、无存量 Profile；这里只代表 Ubuntu 开发环境，不代表生产部署。
- **MF0-A/B 媒体地基已完成（2026-07-17）**：MF0-A 新增 `AssetVariant`、复合 provenance、RLS/FORCE RLS、单输出 recipe、严格 key/checksum/status 与 `derivedKeys` 兼容投影。MF0-B 新增 Profile 三面 + 当前 `activeVersionId` SiteSpec 的 fail-closed 扫描、`409 ASSET_IN_USE` 稳定 usages、Profile/指针/Variant/DELETE 共享 Asset 锁与 Variant trigger；canonical producer 由 key-level advisory lock 关闭同 hash TOCTOU，schema v2 冻结≤128个无环 Variant，Temporal 两轮 Delete+HEAD 后叶→根删行并 durable settle，settled 旧事件重放不碰 replacement。历史 v1 parked 默认 dry-run 分类并生成带 causation 的 v2 successor；迁移前无账本 tombstone 用 `cleanupLegacyUnbound` 隔离，新写不能伪造，lifecycle CHECK 已 validated。Ubuntu 开发库 46 migrations、schema diff=0、真 PG 并发/对账与真 MinIO+Temporal cleanup/replay 均绿；不代表生产部署。
- **M1-c 纯 Sharp 图片管线已完成（2026-07-17 当前交付分支）**：API 直接精确锁定 Sharp 0.35.0，`pipelineVersion` 同时固定 libvips；≤20 MiB/40 MP/4 channels/单页严格解码，自动方向、sRGB、默认剥 EXIF/GPS/XMP，模糊/曝光/噪点只产生版本化 warning。按 kind→role 与显式 focal point 做不放大裁切，输出 320/640/960/1440/1920 中源图可承受的 AVIF+WebP+JPEG/PNG fallback；logo/cert 三编码均无损。inspection 与编码全部在 `concurrency=1/cache=false` 且可超时终止的子进程内；父进程严格校验协议、路径、regular/non-symlink、MIME magic/hash/bytes，进程级门默认并发 1，单输出/整批/result 有硬上限；Ubuntu 编译子进程另有 `prlimit`，但这仍是开发 containment，**不是生产容器/cgroup/独立 UID/禁网沙箱**。writer 在首次对象写前为完整 recipe set 持久预占带 producer token/lease 的 `processing` 行；过期 producer 只能写带一日 lifecycle tag 的 token 隔离 `variant-attempts` key，重新锁定 Asset、校验 token 并取得 canonical key fence 后才可在 15 秒有界事务窗口内 promote，canonical copy 显式剥离 TTL tag。ready 账本缺对象/身份不符时 fail-closed；attempt 在重试前以 8 路有界 IO 删除并压缩，settled cleanup 重放只重删冻结 attempt，S3 调用接 Temporal cancellation+110 秒 deadline。attempt key 写入 durable metadata/cleanup plan；ready set 与 manifest 仍原子收口，失败 CAS 只作用于本 token；单 Asset 最多 120 个 Variant 行，新 attempt-aware cleanup 总对象≤128（历史无 attempt payload 兼容 128 Variant+canonical）。bucket lifecycle 在生产默认 validate-only、缺失阻止启动，仅单一部署 owner 可管理。共享 recipe 保留 MF0-A 旧合同，并以显式 V2 承载完整 operations identity。refurbish P2 首个 Activity 一次物化≤512 个排序 Asset ID 的不可变 workset，后续按两张/Activity 处理；批间 INSERT 不混入、既有成员 UPDATE 不漏项，超限在启动 Sharp 前显式降级。旧 cursor/finalize payload/replay 保持兼容，逐图降级且取消在 commit point 前穿透；`hasPerson` 只保留已有真值，`aiEdited=false`。真 Ubuntu 开发 PG/app_user RLS/MinIO 验证 30 项断言：同 key 双 producer、30 变体、零编码重放、PUT 响应丢失、写前 owner、中断只落 attempt key、接管、冻结 ID workset、容量门、坏图隔离、RLS 与残留清零；不代表生产部署，生产还须独立验 lifecycle、容器隔离、权限、容量、备份与监控。MediaJob、AssetUsage、rembg、生成式图片、视频、公开 process/select API 与 Renderer `<picture>` 均未加入。
- **R3-A + R3-B1/B2 已完成（2026-07-17 当前交付分支）**：R3-A 为 `SiteBuildRun` 落复合 provenance FK、合法状态 CHECK、每站 active 单飞与 nullable Temporal identity；B1 收口严格请求合同、请求指纹幂等与 `workflowId + firstExecutionRunId` ACK。B2 新增 FORCE RLS 的 `SiteBuildStep` attempt 真值，phase/progress 只前进，旧 attempt 迟到不可覆盖，成功/失败/取消均终态化；`scope=page|section`/`options.pages` 冻结请求时 active `baseVersionId`，只合并目标 page/block 与对应文案键，发布以 active pointer CAS 防并发人工编辑被覆盖。新执行先写 run-scoped staging/隐藏 immutable 本地版本，数据库提交后才以单次原子 symlink rename 切 `.active/<slug>`；CAS/事务失败保留旧预览，提交后崩溃由 Temporal retry 从 durable artifactKey 重建 pointer。这仍不等于 R1-min 的生产对象存储 Release、跨节点恢复与回收。目标缺失/重复/脏 spec fail-closed，stylePreset 不得逃逸局部 scope。非 `en` 仍明确 422，真实多语种生成/Renderer 消费归 M1-d；`costSummary` 仍归 R4-B-min。Ubuntu 开发环境完成真 PostgreSQL app_user/FORCE RLS、隔离真 Temporal 成功/取消验证，夹具/namespace/worker 已清理，**不代表生产部署**。
- **R4-A1 Evidence 2.0 基础已完成（2026-07-17 当前交付分支）**：新增共享 `EvidenceRefV2`、租户范围内不可变 `SiteEvidenceSourceSnapshot` 与 `BrandProfileEvidenceRef`；模型调用前先对 intake/KB/storefront/research 做 PII 清洗、规范化、完整 SHA-256 和有界冻结，KB provenance 精确到 document/asset/chunk/hash。intake website URL 也先去 credentials/fragment/敏感 query 与 path/query PII；SearXNG 上游 title/snippet/path/query/fragment 不进入冻结语料或 provenance，只按站主公司名 + external origin 生成最小 `research_hint`，持久化边界再次重建该 hint，并跳过无法证明已最小化的历史 `KbDocument.source=web_research` 正文，防第三方具名个人落入不可变账本。新 BrandProfile 写入固定 `evidenceSchemaVersion=2`，每条事实必须由服务端绑定已冻结 source/type/hash 与 8–512 code point 精确 quote/selector；旧 v1 行继续可读且不伪造回填。两张新表 FORCE RLS、应用角色仅 `SELECT/INSERT`，复合 FK 锁定 workspace/site/profile/source/hash，UPDATE trigger 拒绝改写；删除仍由 owner 沿 Site retention cascade。迁移为 additive/forward-only，回退须先部署 v1 reader/writer，并只在证明无 v2 行后另作 forward migration。无公开 API/OpenAPI 变化。最终 head 真机 verifier 已完整通过：真 PostgreSQL 两账本的 ENABLE+FORCE RLS、`app_user` 仅 SELECT/INSERT、A/B/unset 隔离、跨租户 FK 拒绝与 UPDATE trigger 全绿；真实 SearXNG+Crawl4AI 返回 storefront + 5 个 origin-only web hint，逐项验证无原始 title/snippet provenance，BGE-M3 KB 摄入完成；DeepSeek Pro 两轮 Activity 分别追加 BrandProfile v2/v3，全部事实的关系 ref、quote selector 与 source hash 均通过，重复 intake/KB snapshot 正确去重。第二轮一次研究降级被显式标记后安全继续；verifier 自证隔离夹具清零。此前 DeepSeek 150 秒超时与 GLM `finish_reason=length` 仍证明模型通道会波动，方舟 11/11 连通也不等于 task-shaped 稳定；上述只代表 Ubuntu 开发环境，不代表生产部署。**A1 不证明事实为真**：Claim/Evidence truth bridge、value/quote 语义对齐、任何 `research_hint` 不可发布、cert Asset/人工 verified 发布门均属 R4-A2；BrandProfile 重试复用与成本真值属 R4-B-min。
- **已知独立漂移（不并入 R4-A1）**：空库 schema diff 仍显示 R3-B2 的 `site_build_step.id` / `updated_at` 数据库默认表达式与 Prisma client-side 默认声明不同；R4-A1 新表自身无 drift。该项须独立判定和修复，不能借 Evidence 迁移顺手改变 R3 行为。
- **DOC-12 状态**：#119/#120 已把主要内容分发进 00–14 并登记 ADR-013~019；2026-07-17 新增 ADR-020 记录后续模型组合决策。v3.1/v3.2 已降为 dated proposal。此前把“内容分发”写成“全项目真值已收口”过早；2026-07-16 truth-sync 已纠正权威层与接入文档，未把旧 Word/研究稿升级为权威。
- **当前关键路径**：**R1-safety ①+②、R2-A1–A4、MF0-A/B、M1-c、R3-A、R3-B1/B2 与 R4-A1 已完成**；下一步依次收口 **R4-A2 Claim/Evidence truth bridge → R4-B-min**，之后进入 **M1-d 多语种事实受限文案**并激活非 en build。R3-B2 的本地原子 pointer 已完成；R1-min 剩余的是生产对象存储 Release、跨节点恢复/回收与 unknown component fail-closed，仍须在 M1-e 可见预览前完成。
- **抓取出口 as-built**：Ubuntu mihomo fake-IP 兼容不再使用 broad allow-internal。只有系统解析结果全部落入 `198.18.0.0/15` 时才走固定 Cloudflare DoH；API 连接钉扎并逐跳重验 redirect，Crawl4AI 保留 global-unicast seed guard 与浏览器 pinning proxy。公网 `/md`/`/crawl` 正例和 private/loopback/metadata/IPv4-mapped/redirect-to-metadata 负例均已通过；`:11235` 仍只绑 loopback。
- **已拍板、尚按消费者推进**：26 型封闭组件库（ADR-015）· 模型档四态路由（ADR-016）· MF-0 薄媒体地基（ADR-018）· Readdy 净室参考（ADR-019）· 不可变 Release（ADR-013）· 质量优先目标模型组合（ADR-020）。ADR-020 的组合是 approved `targetCandidate`，不是 current/promoted；切换仍须 ADR-016 的 capability probe、Golden Set、shadow/canary 与回滚门。ACCEPTED 不等于已实现。
- **协同事实源**：[00-decisions-and-coordination.md](../site-builder/00-decisions-and-coordination.md)。承重决策只认 [ADR 注册表](../adr/registry.md)，施工序看 [09](../site-builder/09-m1-implementation-design.md)。

---

## 真实具备（获客快照·冻结 2026-07-13；9 组，均有当时的真实数据验证记录）

企业理解（Claim/Evidence/冲突/人审）· ICP（规则/回测/查询计划/CPV/FDA 映射）· 发现（public_web/wikidata/OSM/名录/展会/TED/openFDA）· 富集（GLEIF/Wikidata/数字足迹/结构化收割）· 采集监控（4 表+增量 diff）· 买家信号（**一等 Signal**：`source_signal` 平台表两层模型 + `signal_ingest` ingest-once 账本（指纹×6h 窗跨 workspace 只拉一次）+ 状态机（EXPIRED/REVOKED 剔除、撤即脱敏）+ 可复算投影（recompute 同过滤面）；web_watch/TED 招标/510k 清关/决策人/smtp_self）· 资格（六维评分 + **demand_proof 观测维**（需求证据类事件驱动，不进总分待乘法门 backtest）/Reachability 硬门/四队列/人工裁决）· **事件交付**（LeadQualified 快照 v1 契约（demand_proof 已填真值，rule=additive-6dim-v2）/`outbox_delivery` 账本/`GET /events` 拉取+ACK/可选 HMAC webhook）· 平台地基（JWKS 座/RLS/Outbox/Temporal 4 Schedule/模型网关/API 门户 + **统一信封契约**：code-first openapi.json 单一真值、CI 契约三道门 + **ToolBroker 唯一执行闸门**：13 个 L0 工具收编全部主链出网、source_policy fail-closed 分层、预算 reserve-then-settle 真拦截、ExecutionContext 贯穿真租户归账）。

## as-built 缺口（8 项，已核验；#1–#5 已修，#7 大部完成；剩 #6/#7 follow-up/#8）

见 [../architecture/current.md](../architecture/current.md) §8。**#1 Fit 聚合根已修（PR #43，收口①完成）**；**#2 Broker 非唯一闸门已修（PR #51，收口②完成）**——13 个 L0 工具收编 22 处直连出网（发现/富集/intent/采集/理解五链全经 `broker.invoke`，例外四类登记），source_policy `required|advisory|none` 分层 fail-closed + 用途门按调用 purpose 判，预算真开账（run/sweep 开关账 + LLM 网关 reserve-then-settle 按 token 折算 + 截断显性化 run 转 PARTIAL），伪 workspace 清零、AI trace 真库实证写入成功；**#3 Outbox 假发布已修（PR #46，收口③完成）**——LeadQualified 现以快照 v1 契约经交付账本真实交付，未注册事件 parked 不再假发布；**#4 OpenAPI 双源已修（PR #48，收口④完成）**——统一信封定稿（`{data}`/`{data,page:{next_cursor,has_more}}`/`{error}`），旧 YAML 删除、契约唯一真值=code-first `openapi.json`，CI contracts job（drift+spectral+oasdiff breaking，label 放行）拦截破坏性变更；**#5 Intent 非一等事实已修（PR #56，收口⑤完成）**——`source_signal` 一等信号表（零个人数据、双时间轴、状态机+TTL）+ `signal_ingest` ingest-once 账本（同源同参同 6h 窗跨 workspace 只拉一次），TED/openFDA 投影反转为只读平台表、sweep 四段化，`attributes.intent` 降为可复算投影（recompute surfaces 同过滤面 + mergeIntent epoch 归一），快照 demand_proof 真值填充（v1 槽位零破坏，rule→additive-6dim-v2）。

**收口⑤记档项**（不阻塞）：ingest PENDING 抢锁根治（TOCTOU 双拉窗口，ERROR 覆盖 OK 已修）· TED CPV 前缀列+GIN 下推（现扫描窗打满有显性告警）· 投影 maxCompanies 游标化（现 subjectsTruncated 可观测，与缺口#8 同类）· license 值 SPDX 统一（随收口⑥ 权利词表）· SUSPENDED=停采不停用两级撤停语义已文档化（存量处置=revokeByProvider）。**#7 存储合规主体已修（收口⑥，PR-A #60 + 后续删除编排）**——`DataRightsService` 7 动作确定性引擎 + jurisdiction_policy(含 PIPL) + policy_decision_log/lia_record/article14_notice + PII 列级加密 + DB 角色拆分；`deletionWorkflow`(GDPR Art.17 冻结→擦除→重评分→回执) + `deletion_request`(状态机 RLS) + `deletion_receipt`(append-only + FK RESTRICT 防级联删) + `POST/GET /deletion-requests` + 事务性 outbox 起编排 + DeletionCompleted 事件。历史 DSR 全链演练记录保留在 changelog/PR；当前测试状态以实际 CI 为准。

仍需诚实登记：**#6** 身份图最小版（merge/split/回放）未落；**#7 follow-up** 的 consent/retention sweep/roles→scopes 仍待；**#8** 跨-sweep 游标公平性仍待。获客恢复商业试点前，还须满足 [release-plan](../roadmap/release-plan.md) 的真实企业 E2E/UAT 与“输出被下一能力真实消费”封版条件。

## 与 PRD v3.0 的登记偏差（8 条，不再静默）

new-api 替代 LiteLLM · 全 TS 无 Python worker · 不用 BullMQ · 自建 ai_trace 替代 Langfuse · OPA 未上（PolicyPort 过渡）· 免费源优先替代付费瀑布 · AiToEarn/触达不在本仓 · recommended 队列门（总分≥0.55 且 Reachability>0）为 PRD 缺位的实现口径。

## 待拍板（3 项 + 1 已落地）

1. ~~制裁名单筛查立项~~ **✅ 已落地（#104，2026-07-14）**——OFAC(SDN+Consolidated)/EU FSF 公司实体筛查作 qualify 第五门，命中→`sanctions_hold` + decide 硬拦，默认 DISABLED（翻 ENABLED 免 LIA=不物化 PII）。ADR-010 已更新记档。
2. **DLP 提单前提核实**——「海关提单」免费方案的前提（DLP FOIA 数据集可再分发）疑似 2023-04 已被拒。需人工上 data-liberation-project.org 核实（10 分钟），结论记入本文件。R3 排期前完成。
3. **两份 v3.0 Word 最小整改**——修撞号/损坏标题/声明重复章节以 PRD 为准/裁决 M2 口径（QGO vs SAO）。预计 1-2 天；**当前随获客侧一起暂停，不分配执行 owner**，用户明确恢复后再由 Codex 实施、用户验收。与交付包「冻结 v3.0」不冲突（只修结构便于迁移引用）。
4. **首发 Job 二选一**——进口商/采购商发现 vs 经销商招募。现有数据源覆盖明显偏向前者（TED/openFDA/提单/web_watch 求供应商页）。建议：**首发「进口商/采购商发现」**，经销商招募 R3 用 Pack 加入。

**需 A/B 会签（用户方向已认）**：PDR-001 对象词典 / PDR-002 业务层级（GrowthInitiative）/ 三接缝契约 / 对象级 RACI。
