# architecture/current —— 本仓顶层架构（as-built + 目标收敛 · L1/L4）

> 文档 ID：`DOC-ARCH-001`
> 生命周期：`CURRENT`
> 当前事实来源：当前代码、共享合同与 [当前状态](../status/current.md)。
> 2026-07-10 v2（合流定稿）。上游：[../product-scope.md](../product-scope.md)（边界与决策）、[../adr/registry.md](../adr/registry.md)（决策注册表）。缺口的整改排期见 [../roadmap/release-plan.md](../roadmap/release-plan.md)。
> **2026-07-16 补，2026-07-27 模型候选重基线同步**：本文 §1–§8 主体描述**获客后端**（C 核心）as-built 架构。自 2026-07-13 起主线转为**独立站建设子系统（Site Builder）**；其 as-built 快照见下方 §1A，细节与承重决策见 [../site-builder/02-architecture.md](../site-builder/02-architecture.md)、[../site-builder/09-m1-implementation-design.md](../site-builder/09-m1-implementation-design.md) 和 [../adr/registry.md](../adr/registry.md) ADR-013~021。旧 Word、v3.1/v3.2 和研究稿不是 as-built 权威。
> **2026-08-02 获客解冻说明**：用户已提前解除获客侧新增开发冻结；M1 收口前只做审计/规划准备，M1 收口后才恢复实现。这不改变本文记录的 as-built 能力、产品边界、provider 默认状态或历史验证新鲜度；获客首项仍须另做当前性审计。
> **2026-08-04 ADR-023 Runtime 覆盖说明**：new-api 仍是唯一模型网络出口；业务调用先经版本化 Task Contract → Context Engine → Prompt/Execution Runtime，再由保留原生 Responses/Messages/Google 能力的 adapter 访问网关。Site Builder 七 ID 中仅 BrandProfile/Copy generative，其余五个任务 deterministic；旧 harness runner/fee-card/fixed-digest 代码退役。精确缓存必须绑定 workspace/build/task/prompt/schema/input/context/model/protocol/reasoning/locale/repair 与 known settlement。Langfuse 只收脱敏 OTel 元数据并 fail-open。下方旧 manifest、费用卡与七路路由描述仅为历史 provenance，不再是执行计划。
> **2026-08-07 Copy capability v8 runtime envelope 与 manifest（覆盖下方 current-source v7 描述）**：v7 真实运行只到 Terra/Responses 首 wire；120 秒 AbortSignal 先于 new-api/OpenOx 的 127.554 秒 Cloudflare 524 返回，故账本按 `requestId=null`、unknown settlement 冻结，Sol/Sonnet 未调用。new-api 最终退款只证明网关 token quota 已恢复，不能代替逐 wire provider settlement。v8 保留原生 Responses/Messages、候选/协议/reasoning、3/6/1、子 campaign 隔离与所有 admission/ledger/settlement 门，只把 Copy 的 max output 从 4,000 收紧为 1,200，并把有界观察窗延长为 240 秒。该源码已由 [`v8 fixed-source create-only manifest`](../evidence/site-builder/m1-g-copy-real-capability-manifest-v8.json) 固定到 `719aacc2ec328870316b71cb0666d22828b89e74`；manifest 仍为 `NOT_AUTHORIZED`、0 wire、0 cost，不代表模型可用、候选晋级或 route change。
> **2026-08-07 Copy versioned evidence acceptance（当前交付分支；覆盖下方 Ed25519 可运营性描述，但不改其历史实现事实）**：#332 已把 v8 create-only manifest 合入 `origin/main@0f6d31a0cb62dd061a22f0c82e40414e831c486d`，在该源码上的 post-merge 零调用 preflight 通过；现行 v8 最终 evidence 仍依赖当前执行环境未提供、仓库/常规安全引用中也未发现的对应 Ed25519 私钥或 signer，故只能到 `copy_gateway_settlement_candidate`。v9 Runtime binding 移除 Ed25519 执行依赖但不直接把 candidate 冒充 accepted evidence：exact authorization、finite exact credential、fixed source/manifest/source bundle、compiled binding、execution/input/context/prompt/output/model/protocol/reasoning digest、one-shot ledger 与逐 wire known settlement 全部成立后，runner 只产包含脱敏 settlement chain、campaign/ledger authorization 和 execution-time evidence binding 的 candidate；compiled runtime、plan/input/context/prompt、fixture/child slot、model/protocol/reasoning 与 authorization digest 都同时锚定在原 `campaign_opened` hash-chain 事实。`git-reviewed-evidence-acceptance/2026-08-07-v1` 再以 create-only artifact 固定该 candidate；verifier 机械要求单次新增、无后续修改、working/HEAD/`origin/main` 字节一致和 main first-parent PR merge provenance，Copy exact schema 拒绝未知或 secret/raw-content 字段，runner 复核所有 binding 后才写 `git_evidence_acceptance_consumed` 并生成 `git_reviewed_gateway_settlement_accepted`。真实 dispatch 仍须 live admission/source/compiled/gateway；post-merge evidence-only reopen 则只有 ledger/claim/marker 与 Git acceptance surface，没有 `execute()`、bearer、network、current-plan 或 current-expiry 依赖。必需 CI 与独立 reviewer provenance 属 main-bound PR 的另行治理证据，不由本地 Git verifier 冒充证明。该链可跨进程接受 capability pilot；质量 evaluator v5 则仍依赖原执行进程内的 result/metadata WeakMap，尚不能跨 acceptance merge/restart 安全重放输出正文，因此 36/72 在 restart-safe durable accepted-execution/output-byte artifact 与一次性消费落地前保持 BLOCKED。v9 manifest fixed source 只有迁移合并后才能填写；当前 v8 仅为历史且 fail-closed，下一步仍是 post-merge v9 create-only manifest + 与其精确绑定的 dispatch 授权。当前没有模型调用、promotion、route adoption 或生产部署变化。
> **2026-08-07 Copy v9 fixed-source create-only manifest（当前覆盖上方待生成描述）**：[`v9 manifest`](../evidence/site-builder/m1-g-copy-real-capability-manifest-v9.json) 固定 #333 merge `5775945c6e056e99ba9357d8d8794b01fad0c66b` 与 clean prep head `118d657d9ed3f43228a9ebf7d960e7c7fb884b17`，其 68-file bundle digest=`9dc4fed3…`、artifact digest=`f4eaa8e…`。bundle 覆盖版本化 Git-reviewed evidence acceptance 与当前 Runtime/Copy/contracts source，不再包含退役的 operator key/authorization；manifest 保留 3/6/1、native Responses/Messages、候选/协议/reasoning，且仍是 `NOT_AUTHORIZED`、0 network/model wire、0 cost。生成器不读 credential、`.env` 或 new-api；3,000,000 quota points 仍是未来 purpose-specific finite/exact credential attestation 与独立 dispatch authorization 的 live 边界，不是当前 artifact 观察值。该 manifest 不产生 capability/quality evidence，不改变 promotion、runtime route 或生产部署。
> **2026-08-07 Copy quality durable replay（当前交付分支，zero-call）**：质量矩阵沿用 Runtime 与 new-api 的真实调用链，但将“调用”和“合并后接受/评分”拆成可重启两阶段。共享 36/72 append-only ledger 在 `campaign_opened` hash-chain 中先固定 purpose/topology、fixed source、manifest/admission、credential/settlement、compiled runtime 和 exact caps；execution 完成后再形成绑定 canonical output bytes（≤64 KiB）、candidate/fixture/repeat 与 known-settlement 的 candidate receipt。main-bound `quality_matrix` artifact 经 Git provenance 通过后，evidence-only reopen 读取该 execution 的历史 completion snapshot，逐字节复核输出、与授权的重叠 provenance 对齐，并以 `git_accepted_output_replay_consumed` 一次性消费。正文只存在于受审 artifact，ledger 只保存 digest/length；capability acceptance 与 quality replay 共用 acceptance/execution/artifact 身份命名空间，阻止跨 evidence kind 重放。evaluator 的 replay-backed provenance 通过私有 WeakSet 传播到 review/stability，只有全量 replay-backed 聚合才移除 durable blocker；process-local/fake/mixed evidence 继续拒绝，promotion 与 production route adoption 不在该状态机内。历史 quality manifest v1 不含这一架构，后续 v2 必须在本分支合并后的 fixed main 上生成。该技术门不授权 v9 或 36/72，不调用模型/网关，也不改变凭据、晋级、路由或部署。

> **2026-08-06 Copy capability runtime（#319 + #321 + #323 + #326 + current-source v7）**：#317 的 closed repair、finite credential、fixed source、compiled guard 与 request-bound settlement 继续只生成 `copy_gateway_settlement_candidate`。#319 的 pilot runner 使用仓库固定 Ed25519 公钥；#321 以 new-api boolean allowlist 和 token-scoped exact catalog 同时约束 Terra/Sol/Sonnet；#323 把 AI SDK API-call failure 规范化为不含请求体/响应体的有界 HTTP 状态、request ID、retryable、响应长度与摘要。#326 再把三个候选拆为独立 authorization、reservation、ledger identity 和 campaign，候选 unknown settlement 只冻结自身；batch guard 对共享 source、manifest、compiled runtime、credential 与全局授权漂移执行三账本全冻结。只有当前 runner 的 candidate、当前 verifier 的 authorization 与同一子账本共同生成的 opaque handle 才能成为 `real_gateway_settled` capability evidence；通用 setter、不受信任 key injection、对象 clone、自签、跨候选 permit 与重复消费均不存在。主机信任边界仍是无未授权同进程代码的 compiled Node 进程（包括 preload、loader hook、`require.cache` 替换与 native intrinsic mutation）。[v7 manifest](../evidence/site-builder/m1-g-copy-real-capability-manifest-v7.json) 固定 #326 merge `a0dde901…`、69-file source bundle `7c23b6bd…` 与 artifact `2b66605a…`，create-only artifact 保持 `NOT_AUTHORIZED`。先前真实 v6 campaign 的 Terra/Responses 首 wire 在 OpenOx HTTP 502 后以 unknown settlement 冻结，只证明诊断链生效；它没有产生 capability evidence、晋级或 route change，且不能为 v7 复用授权。
> **2026-08-03 source/manifest split（覆盖下方旧 harness truth-sync）**：`site-builder-model-evaluation-harness/2026-08-01-v18` / `design_spec` suite v15 源码已随 #266 合并；矩阵仍为 73 executions、最多 146 wire calls；单个物理调用与整次 execution reservation 分层结算，probe attestation 也使用完整 execution reservation，最终评分绑定捕获的 validator。最初零费用 manifest 固定的 `295038d323b4bd09ed16ab73ea981d24e1f010df` 已仅为历史 source；当前可执行 source 由下述 v6 指定。当前无真实调用、费用、evidence、promotion 或 active route 变化。
> **2026-08-04 current fixed source refresh**：#282 的 suite admission 改变 shared harness/executor 后，新的 [v6 manifest](../evidence/site-builder/m1-g-design-spec-evaluation-manifest-v6.json) 以 `5c37bb9270db6893144f07c2431e74a830d6b9f4` 为唯一可执行 source，绑定 v15 source bundle `c6deda…`、73/146、deterministic comparator 与 post-build contracts attestation；prepare contract 仍为 `site-builder-design-spec-evaluation-manifest-prep/2026-08-03-v2`。它未读取凭据、余额、`.env` 或 OpenOx，零网络/模型调用、零费用、`NOT_AUTHORIZED`；v1–v5 仅保留审计，不能跨 source 执行。
> **2026-08-04 v6 public-price card**：该 source 的 [v6 price card](../evidence/site-builder/m1-g-design-spec-v6-native-fee-card-2026-08-04.json) 已从 OpenOx 公共目录冻结 CNY `11.276659` + USD `3.45842784` 的分币种机械上限和目录响应摘要，仍为零模型 calls/零实际费用/`NOT_AUTHORIZED`。执行 runner 仅信任 v6 preflight attestation，旧 v5 在 client/ledger 前拒绝。它不能证明凭据、余额、known settlement、真实 evidence、promotion 或 active route。
> **历史 2026-08-03 `design_spec` v2 manifest/price card**：v2 与 suite/source bundle v15 绑定，生成 dispatch=`NOT_AUTHORIZED` 的 fixed-source manifest；[v2 price card](../evidence/site-builder/m1-g-design-spec-v2-native-fee-card-2026-08-03.json) 从 OpenOx 公共目录冻结 CNY `11.276659` 与 USD `3.45842784` 的历史 73/146 原币上限。它们保持零模型调用、零实际模型费用和 `NOT_AUTHORIZED`，不构成 credential attestation、执行授权或 runtime readiness，也不能跨 source 替代 v6。
> [v2 machine manifest](../evidence/site-builder/m1-g-design-spec-evaluation-manifest-v2.json) 与 [v2 零费用决策卡](../evidence/site-builder/m1-g-design-spec-evaluation-manifest-v2-decision-card.md)只供审计；它们当时绑定的历史运行合同为 `site-builder-model-evaluation-harness/2026-08-01-v18`、候选基线 `site-builder-model-candidate-baseline/2026-08-04-v1` 与成本安全合同 `site-builder-model-evaluation-cost-safety/2026-07-30-v2`，均不再是当前可执行评测链。
> **2026-08-04 全文本任务 suite admission**：copy、assemble、assembly_fix、qa_summarize 和 seo_review 均已具有 canonical zero-call suite。copy 通过生产 Claim/slot canonicalization 与 CopyBundle finalization 判分；assemble/assembly_fix 各覆盖 M1-e-B 的 12 个 sparse/rich golden fixture，冻结模型可见的全量 page/section targets，拒绝空、遗漏或非 canonical selection，再经 selection→SiteSpec→production validator 闭合；其 source bundle 逐项绑定 renderer component-qualification JSON fixture，repair 额外绑定 prior candidate digest 和 findings；QA/SEO 各有 2 个 strict deterministic summary fixture。全部 legacy comparator allowlist 为空。该基础未读取凭据或模型、没有 evidence、promotion、runtime route/fallback 变更；所有任务仍需逐项 fixed-commit evidence 与 promotion。
> **2026-08-04 M1-g diagnostic closeout**：后续 campaign 完成 206 executions / 262 wires，但不是 canonical fixed-HEAD/cost-safe executor 产物，也没有内嵌 resolved upstream/channel 或逐 wire settlement attestation；机器文件因此固定为 `diagnostic_only`、`promotionEligible=false`，并把 62 accepted、99 validator rejected、45 transport failed 分离。架构上没有任何候选晋级、runtime token、active route 或 rollback 变化；一次性 runner 已删除。

## 1. 顶层运行架构

```
External SaaS（身份 / UI / Campaign / Outreach / Opportunity·QGO）
        │ JWKS token + REST          ▲ 领域事件（按 sink 投递+ACK）
        ▼                            │
NestJS Interface Layer（B：Auth context / Controllers / OpenAPI）
        ▼ 方法调用
Modular Acquisition Core（C：Seller / ICP / Discovery / Identity / Signals / Contacts / Qualification）
        ▼
Temporal Workflows（编排；业务状态在 Postgres）
        ▼
ToolBroker / Execution Gateway（唯一执行闸门——✅ 已收口，PR #51：13 工具 + fail-closed + 预算 + Trace）
        ▼
ProviderAdapters / Anti-Corruption Layer
        ▼
TED / openFDA / Web / Crawl4AI / SearXNG / GLEIF / Wikidata / SMTP ／ new-api 模型网关
```

物理形态：模块化单体 + 单 PostgreSQL；进程可按资源特征拆（API/workflow worker/crawl worker/outbox delivery worker）——**资源隔离，不是微服务化**（ADR-002）。

## 1A. Site Builder as-built 与演进门

```
SaaS / API Client
        │ JWKS + REST
        ▼
NestJS site-builder context（intake / site / profile / asset / KB / build）
        │                         │
        ▼                         ▼
PostgreSQL + RLS             MinIO（素材/KB 对象）
        │                         ▲
        ▼                         │
Temporal 固定 DAG → Brand/Asset → DesignBrief → CopyBundle → 受控 Assembly
                                                               │
                                      SiteSpec 1.0(Demo) / 1.1 ─┘
                                                               ▼
                                    Astro renderer → SiteRelease v1/v2 → active pointer
```

- 类型边界：`@global/contracts` 的 `SiteSpec = SiteSpecV1 | SiteSpecV1_1` 是 API 与 Astro 的唯一共享类型。Demo v0 显式固定 1.0；M1-e-B 新历史固定 1.1 并增加组件库、Renderer、archetype、Family 与 `dirByLocale` identity。ReleaseManifest 同样为 v1/v2 closed union；旧版本不后台迁移。
- #121/#123/#124/#126 已收口 intake 行为、事实安全、隐私、可取消超时、失败保站、幂等合同与 OpenAPI；R1-safety、R2-A1–A4、MF0-A/B、M1-c、R3-A/B1/B2 与 R1-min 均已落地。新构建以 `SiteRelease` manifest/digest 写入 S3-compatible producer-isolated 不可变对象，READY 后才允许 `Site.activeVersionId` 事务晋级；公开预览从该唯一指针解析对象并复验 digest。历史 `local:` 版本不静默回填，也不能冒充生产 Release。
- R4-A1 已新增内部 `EvidenceRefV2` 合同、不可变 `SiteEvidenceSourceSnapshot`/`BrandProfileEvidenceRef`、FORCE RLS 与复合 provenance FK。P1 Activity 在模型前冻结经 PII 清洗和规范化的 intake/KB/storefront/research 语料，KB 精确到 chunk hash；新事实必须精确绑定 source/type/SHA-256/quote/Unicode selector。旧 v1 BrandProfile 不伪造回填；公开 OpenAPI 未变化。R4-A1 只证明引用完整性，不证明事实真值或可发布性。
- R4-A2 在 BrandProfile 持久化边界加入公共企业事实 allowlist、PII/未消歧案例与 value/quote 关键值门，`research_hint` 永不投影；新 intake 原子建立 CompanyProfile→Site 复合租户关系，历史未关联 Site 不按名称猜身份。fact key 唯一合同为严格 lower_snake_case，schema/gate/投影与 EvidenceRef/Claim 约束均拒绝非 canonical 输入。通过项以稳定分域 key 复用共享 Claim/Evidence，不可变 FORCE RLS bridge 与 trigger 精确绑定 company/fact/snapshot/hash/quote/selector/asset；认证还须 live ready cert Asset。origin Claim 审批以 Claim 行锁+exact bridge 预检开始，固定 search_path 的 security-definer trigger 在 UPDATE 内取得 surviving bridge 行锁；API 绕过与 Site/BrandProfile 级联后的孤立审批均 fail-closed，manual/legacy null identity 仍兼容。Workspace 仍有 Site 时物理删除被 trigger 拒绝，避免 relational cascade 绕过对象清理/outbox；Asset DELETE 的 cert bridge 扫描由 partial ordered covering index 支撑。审批、撤销、到期与冲突裁决继续以 CAS/固定锁序把状态和 outbox 原子成对。A2 本身没有新增 endpoint、SiteSpec/CopyBundle 或消费者；后续 M1-d 已在其上建立正式消费链。19 个 A2 forward migration 只在 Ubuntu 开发环境通过空库/旧数据/真 PG 验证，不代表生产部署，恢复快照仍须逐 migration 通过 5 秒 lock gate。
- R4-B-min 为每个 BuildRun 原子创建 `SiteBuildBudget`，以 `SiteBuildTaskAttempt` 的 lease/fence 管 logical task，以 `SiteBuildSpend` 的稳定 operation key 管 physical model/tool call；数据库函数串行 reserve/settle/reconcile，结算 ACK 不明保持 unknown 并阻止 fallback 重复花费。BrandProfile 在外部 IO 前 claim，冻结 input/output，重放复用成功产物并以 attempt 复合 provenance 原子完成。模型网关与 ToolBroker 共用账本；provider-reported、MODEL-1 冻结 token pricing、tool-reported、legacy estimate、unknown/not-incurred 分层，预算保守 charge 不冒充真实 cost。取消/预算/非运行态关闭付费门，终态写闭合 `site-builder-cost-summary/v1`。该合同已通过 Ubuntu 真 PostgreSQL RLS/并发/ACK-loss/replay/kill-switch verifier，不代表生产部署，也不改变 MODEL-1 路由证据。
- **历史 2026-07-29 settlement fast-follow** 曾要求七 task 的模型 route 都由短期 attestation 覆盖；该设计现已被 ADR-023 的统一 Runtime 与五个 deterministic task 收敛覆盖，不再描述当前可执行矩阵。它保留 unknown settlement fail-closed、known per-wire observation 等历史 provenance，但当时缺价的 MiniMax/Doubao/DeepSeek Flash 已不可借此恢复为 active route。
- 模型面须分层描述：现役只认 `task-routes.ts`；7 个 task 中仅 `site_builder.brand_profile` 经真实 new-api 协议探针、同形 6×2 Golden 与成本/质量门成为代码级 `promotedRoute`=`gpt-5.6-terra`（Responses）→`claude-sonnet-5`（Messages），并有任务硬门和回 DeepSeek Pro→GLM 开关；Copy 仍是 DeepSeek Pro→GLM `currentRoute`。`design_spec`、`assemble` 与 `assembly_fix` 均为 `safe-blueprint`，QA/SEO 均为 `rule-summary`，五者是零模型调用的 `deterministicFallback`。MiniMax/Doubao 仅存于不可执行历史 provenance。2026-07-19 fast-follow active evidence id=`model1-brand-profile-20260719-v20`：final-code candidate 24/24（两模型各 12/12）且完整 legacy route baseline 12/12，同 source bundle，并精确记录 requested/reported/resolved model 与 transport；旧失败/诊断证据保留。三路全过后仍按 accepted-artifact 成本和失败门保持 Terra→Sonnet，不再用旧 baseline 失败作晋级理由。#140 合并后，Ubuntu 开发环境的应用已把 Ollama BGE-M3 以私有别名 `site-builder-bge-m3-local` + 专用模型受限令牌统一经 new-api 调用，并通过真实 `EmbeddingsClient` 两条 1024 维有限向量验证；这不代表生产部署。2026-07-27 开发网关另登记 4 个 OpenOx 分域渠道，并创建按能力域跨供应商划分的文本 23 型、图片 4 型、视频 5 型白名单令牌；重叠 `gpt-image-2` 只由图片渠道承载，`gpt-5.3-codex` 因上游目录缺失未配置。Gemini 文本 key 存在目录/推理漂移：OpenAI Chat 返回未配置 upstream，原生入口 404，故渠道已禁用；allowlist 保留其型号不等于健康。分域令牌尚未接入应用，历史通用令牌仍为宽权限，因此不能声称端到端凭据隔离或运行时切换已完成。Seedance/Grok Video 的 Compose 内网无状态兼容层只桥接 new-api `/v1/videos` 创建路径到 OpenOx `/v1/video/generations`，查询与模型目录透明透传。图片内置测试会真实生成或以错误 Chat 协议失败，不能作为健康门；本轮没有 task-shaped 图片/视频接口验证，不证明响应、质量或成本兼容，也不构成 MediaGateway 或消费者。ADR-020 保留历史决策 provenance；ADR-021 的非运行时候选唯一真值是 `site-builder-model-candidate-baseline/2026-08-04-v2` 及其[生成文档](../site-builder/model-candidate-baseline.md)。registry target 由该基线构造但不可路由，Preview 只能 shadow，逐 task promotion 仍受 ADR-016 约束；BGE 路由迁移不换模型、维度或 `embed_version`，因此不触发重嵌。
- ADR-022 将历史 currentRoute provenance 与 `site-builder-model-rollback-policy/v1` 分离：MiniMax/Doubao 已是 `pending_retirement`，只保留历史 provenance，不得进入 active route、runtime token、attestation、comparator、价格覆盖或可执行 rollback；环境覆盖在 client 前同样拒绝。Gemini 文本继续 `deferred`。六任务仍须逐一完成 fixed-commit evidence 与 promotion。
- **2026-08-03 实施状态**：ADR-022 的退役切除已进入 `task-routes.ts`。MiniMax/Doubao 仅保留在不可执行 legacy snapshot；`design_spec` 在任何 model client 前返回 `safe-blueprint` 确定性回退，copy 删除 Doubao fallback，QA/SEO 删除 Doubao fallback，环境变量覆盖亦不得重新引入 retired alias。该变更不晋级任何候选、不读取凭据、不执行模型或改变 runtime attestation 的未授权状态。
- 离线候选评测现为代码级 `site-builder-model-evaluation-harness/2026-07-28-v3`：evaluation-only executor 只通过依赖注入的 wire/cost seam 连接 canonical harness request，文本协议固定映射 Responses、Messages，并以隔离入口保留 legacy Chat comparator；其 transport admission 与生产 `VERIFIED_GATEWAY_MODEL_TRANSPORTS` 分离。executor 同时绑定 `site-builder-model-evaluation-cost-safety/2026-07-28-v1` 的私有 attestation：专用有限额度凭据、精确 target/comparator alias+protocol、显式冻结价格与 resolver identity、request 级 `executionId` 结算、campaign/execution/wire/prompt/output 绝对上限缺一即在 reserve/client 前拒绝；unknown settlement 冻结 campaign，默认/未配置倍率和媒体通用测试不可用。trusted probe/run 只接受 factory 私有 WeakSet 品牌化并冻结的 target execute，不暴露 register/test seam；任意 callback/wrapper 在预算 reserve 与 client dispatch 前拒绝。媒体、embedding、preview、deferred、legacy-only target、无消费者或无 suite 同样在 client 前拒绝；当前唯一完整 suite 是 BrandProfile。task/protocol/envelope、canonical suite、矩阵、排序、预算与完整 provenance 合同只认[生成评测基线](../site-builder/model-evaluation-harness.md)。当前测试仅使用 fake wire/fetch/settlement，未读 `.env`、未接生产 `ModelGateway`、new-api 配置、Temporal 或媒体消费者；这仍不是 capability 证据、真实评测报告、`evaluatedCandidate` 或路由晋级。
- **2026-07-30 harness truth-sync（覆盖上一条旧 ID/单 suite 描述）**：当前合同为 `site-builder-model-evaluation-harness/2026-08-01-v16`。BrandProfile 既有 suite 保持，`design_spec` v13 suite 固定六 Family × sparse/rich 的 12 个合成生产输入、3 候选 × 2 重复、一次 GPT-5.5 probe 与 24 个零调用 deterministic catalog comparator case；fixture task input 必须通过产品共用的完整 catalog enumeration、required-role eligibility、ranking 与 top-3 projection 重建，输出解释为四种封闭 claim，稳定性覆盖完整输出。执行 comparator allowlist 由 suite 固定为空，不再从历史 current/rollback route 推导，因此 target-only 凭据必须逐字等于 73/146，MiniMax/Doubao extra scope fail-closed。`site-builder-model-evaluation-cost-safety/2026-07-30-v2` 进一步把 spend authorization 固定到准备阶段的 fixed commit、suite、source-bundle contract 与 SHA-256；prepared commit 必须逐字等于执行 worktree 当前 `HEAD`，task system prompt 与 `design_spec` validator identity 均在模块加载时捕获；提交、prompt、validator 或 canonical case digest 不同即在预算/client 前拒绝。create-only runner 用模块加载时捕获的 Array intrinsic 拒绝预加载 compiled contracts runtime，再以私有 build token 将 clean rebuild、构建后 suite import 与最终 attestation 绑定，artifact-tree canonical serializer 不调用实时 `map`/`join`/`sort`，且只写站建 evidence 目录。writer 还会重构 canonical manifest，并逐文件比较当前 worktree、fixed commit blob 与 source digest，错绑在写入前拒绝；fixed source commit 必须是 prep head 的祖先，本 PR 只允许 merge commit 保真，squash/rebase 或合并后不可达会让后续 evidence 在预算/client 前 fail-closed。repair reason 过长与 provider output-token 超限都会先结算并关闭 reservation，再持久写入 authorization freeze。运行时把 21-artifact 文件树、catalog 构造实际使用的全部 contracts 导出代码指纹和 Node 模块实例身份共同绑定到 suite/case/probe/run；首个 wire 前的漂移也会持久冻结 authorization，harness-load 及首调/repair 每个物理 dispatch 的 post-wire guard 在成功与所有失败路径上均执行，漂移须结算并冻结。该 suite 只有 fake-wire 测试和零费用准备，尚无真实 evidence、费用授权、promotion 或 active-route 变化。
- fixed-commit evidence 前置层为 `site-builder-model-evaluation-evidence-prep/2026-07-29-v1`：它只从 canonical suite、legacy route 和受信 `site-builder-model-evaluation-cost-safety/2026-07-28-v1` 派生 create-only manifest、不可逆凭据指纹和费用决策卡，没有 executor/client seam。清单冻结 BrandProfile 61 executions / 最多 122 wire calls、一次 repair 上限、source bundle 和停止条件；61/122/2440¢ 仍是未验证规划上界。safe-snapshot envelope 与 source bundle 必须已由 fixed commit 跟踪并逐文件复核，决策 bundle 保留 spend authorization/ledger identity 及 attestation/envelope digest；只有真实冻结价格、计费单位、精确有限 scope、余额采样和 fixed commit 全部闭合才可输出 `READY_FOR_PRODUCT_DECISION`，同时保持 `dispatchAuthorization=NOT_AUTHORIZED`。`design_spec`、QA/SEO 真实 evidence 及 copy、assemble、assembly_fix、媒体与 embedding 不进入该清单。
- M1-d 已把 R4-A2 bridge 冻结为 immutable PublishableClaimSnapshot，并以 SiteVersion/locale CopyBundle、R4-B task attempt、dual-read SiteSpec 与激活前 Claim 重验形成正式消费者。生成支持 `en/de-DE`，`ar` 只作 RTL renderer smoke；renderer 具备 locale route、自托管字体和构建产物 outbound-domain gate。公网 inquiry 仍未实现。
- M1-e-A/B 已把 55 型资格、六个 approved Family、DesignBrief producer、封闭 assembly adapter/copy slot/四层 validator、内容寻址 asset overlay 与 ReleaseManifest v2 接入真实 refurbish DAG。模型只可建议冻结 ID，服务端纯函数产生最终 props；三轮后只在同 Family 使用 safe Blueprint。Catalog=`m1-e-b/1.0.0`，12 个 sparse/rich Golden 有 36 张三断点整站证据。
- M1-f 将新 history 拆成 `building candidate → P4 quality → materialize Release v3 → active pointer CAS`：完整站点经确定性 QA/SEO/a11y/genericness、375/768/1440 截图、Lighthouse 与 closed-shape DesignEvaluation v2；round 0 后最多三次只接受服务端生成的同 Family `repairOptionId`，模型不获得 SiteSpec props、组件、CSS、HTML、Astro 或 JSON Patch 权限。质量未过、取消、预算关闭或并发 pointer 变化时旧 active Release 不变。最终证据复制到私有 Release quality prefix，公开静态文件清单不含截图/评测 JSON；v1/v2/v3 reader 兼容，v2/v3 partial base 都评测完整站点，v1 base 保持稳定拒绝。
- Temporal patch `site-builder-m1f-quality-loop-v1` 位于原 P3 `assembleAndBuild` 之前；旧 history 保持既有 Activity 命令序列，新 history 只依赖已记录 Activity 结果分支。Release 已 materialize 但 pointer CAS 失败时保留 ready/inactive Release 供幂等重试或 GC；失败候选临时质量对象由 compensation 有界清理。Gemini task-shaped MODEL-1 因超时为 `unavailable`、未晋级，确定性通过只可记为 `passed_deterministic_aesthetic_unavailable`。M1-g 于 2026-08-04 以确定性产品门完成和 206-execution 诊断 campaign 收口；campaign 不具 promotion 资格，所有候选不切路由。这仍不代表公网发布、MODEL-2 或生产部署。
- 🔴 **抓取 egress as-built**：Compose 已移除 broad allow-internal；Crawl4AI 固定不可变镜像 digest，保留 seed global-unicast 守卫和浏览器 pinning proxy。Ubuntu fake-IP 仅在系统答案全部位于 `198.18.0.0/15` 时经固定 Cloudflare DoH 回退；private/loopback/metadata/保留或混合答案 fail-closed。API 的 Crawl/robots/`http.get` 在每一跳解析、校验并钉扎连接，限制 redirect、超时、响应大小且跨域剥离凭证。公网与 private/loopback/metadata/IPv4-mapped/redirect-to-metadata 真机矩阵已全绿；loopback 端口绑定仍保留为 defense-in-depth。

## 1B. 开发认知层（非产品运行时）

仓库内 `@global/code-intelligence` 提供只读 ContractGraph CLI。它从 canonical Traceability Matrix 显式连接业务 Registry 与实现/测试锚，并从 TypeScript/NestJS、Temporal（含 activity factory 实现）、Outbox、Prisma/migration/RLS、Astro、pnpm/tsconfig、Compose/systemd 与 GitHub Actions 确定性生成 `.code-intelligence/` 派生图；每份结果绑定 main 工作区、当前 worktree、branch、commit、commit time、全源 source hash 和派生文件 manifest。查询发现 worktree、哈希、schema 或派生文件完整性不一致时 fail-closed。CI 以双构建字节等价、extractor 存在/非零覆盖和通用动态表面 `UNCLAIMED_DYNAMIC_MECHANISM` 错误门防止新注册机制静默逃逸。

精确固定的 `@colbymchenry/codegraph@1.5.0` 只作为本地只读二级试点：遥测在动态加载前关闭，不运行其 agent/Hook/MCP 自动安装；`origin/main` 每次从 Git archive 写入临时目录、拒绝 symlink 后替换快照，active 则只把 Git 已跟踪/已暂存的规则文件复制到不可变快照，索引后再次核对未跟踪集合和逐文件哈希，窗口内变化即删除派生物并拒绝 evidence。另只允许当前活跃 worktree 一个索引。查询前强制核对版本、branch、commit、source hash、project path、完整索引和 pending change；增量性能结果还必须证明旧符号消失、新符号可读和队列归零。2026-07-25 的固定 30 题按职责路由后统一 precision/recall、精确动态边、分支识别、泄漏和速度门均通过；但 CodeGraph 原始 precision/recall 仅 42.2%/65.5%，在其获准参与的 19 题中 recall 也只有 89.5%，未达到 90% 独立采用门，因此状态仍为 `PILOT_ONLY`，不进入日常默认路径。

该层不进入 API、worker、数据库或生产部署，不索引 Secret/`.env`，`.codegraph/` 与 `.code-intelligence/` 均被 Git 忽略且可随时重建。静态图只能提出影响候选，不能证明运行时 DI、字符串分派、外部消费者或数据库行为真实发生；完整边界与使用顺序见 [代码智能使用与边界](../ai-development/code-intelligence.md)。

开发环境运行证据 CLI 在同一 `@global/code-intelligence` 包内按需执行：只读探测 API/DB health、systemd、`global` Compose、Temporal cluster/Schedule、最新 migration、Outbox 元数据和 SiteBuildRun/Temporal identity，原子写入带 manifest 的 `.code-intelligence/runtime-*.json`。每条证据只保存按 kind 和值格式双重白名单允许的标识、状态、时间、耗时和哈希；任意 Outbox correlation 自由文本不落盘，也不读取 Outbox payload、业务正文、模型输入、Secret 或个人数据。bundle/manifest 绑定 repository、worktree、branch、commit、commit time、clean state 与 source hash，采集前后变动即拒绝，24 小时后失效。差异报告只把 Temporal 最近 Schedule action 认作真实 Schedule→Workflow 边；Outbox 行不冒充消费者已运行。当前服务未暴露部署 commit 与 API correlation 回显时明确输出 `UNKNOWN/PARTIAL`；Compose 只有 running 而无 healthcheck 不冒充健康，三个长驻 systemd unit 的 `active/exited` 也按失败；容器配置来源偏离 canonical 主工作区会单独报告 provenance drift。预发布/生产采集与 OpenTelemetry 均未启用。

## 2. Bounded Contexts（9 个）

| Context                           | 核心对象                                                                                                                                       | 数据归属                                                                                                                                                   |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Access & Delivery                 | RequestContext、Command/Query、Handoff                                                                                                         | B；外部 token 只提供 ID                                                                                                                                    |
| Seller Knowledge                  | SellerProfile(CompanyProfile)、Offering、Claim、ClaimEvidence、KnowledgeConflict                                                               | Workspace/RLS                                                                                                                                              |
| Targeting                         | ICPVersion、QualificationRule、Persona、BuyingCommitteeRole、QueryPlan                                                                         | Workspace/RLS                                                                                                                                              |
| Source Governance & Acquisition   | DataProvider、SourcePolicy、CanonicalTaxonomy、MonitoredSource、SourceFetch/Entity/Change                                                      | 平台共享；**不得含个人数据**                                                                                                                               |
| Discovery & Identity              | DiscoveryRun、RawSourceRecord、Organization(canonical_company)、OrganizationIdentifier、IdentityLink、FieldEvidence                            | Raw/候选按 Workspace/RLS                                                                                                                                   |
| Buyer Signals                     | **Signal（一等，✅ 已建=收口⑤ PR #56：`source_signal` + `signal_ingest` 账本）**、DemandEvidence、WebsiteWatch                                 | 两层：平台级 `source_signal`（绿色公司事实、无 RLS、零个人数据）+ 租户级投影（RLS）；web_watch 类租户注册源归租户层（其一等事实账本=source_entity_change） |
| Contact & Storage Compliance      | CanonicalContact、Employment、ContactPoint、PersonalDataClass、Suppression、Retention/DSR                                                      | Workspace/RLS、列级加密、最小化                                                                                                                            |
| Candidate Qualification & Handoff | **CandidateAssessment（ICP×Organization，已由 Lead 聚合根承载，收口①/#43）**、ScoreSnapshot、Lead、QualificationDecision、LeadQualifiedPackage | Workspace/RLS；事件交付 SaaS                                                                                                                               |
| Runtime Platform                  | Temporal、ToolBroker、ModelGateway、UsageLedger、AITrace、Outbox、Audit                                                                        | 平台基础设施                                                                                                                                               |

命名修正（含交付包 §6.4 语义对齐）：`canonical_company` 实为 **workspace 内** canonical（非全球主档）——短期保留表名、文档明确语义，长期拆「平台级绿色 Organization Identity」与「租户级 Account 投影」；本仓机器产出正式命名 **Qualified Buyer Candidate（Batch）**——`Lead`=Company×ICP 资格评估对象（评分读模型），**用户接受后的「正式 Lead」概念上归平台业务核心**（单库现实下由本仓代管 accept 端点，目标归属入 ADR 备案）；ClaimEvidence/FieldEvidence/SignalEvidence 分开。

## 3. 数据平面与最小权限

- Tenant plane：`workspace_id + RLS`（✅ 已建）。
- Platform reference plane：taxonomy/provider/source_policy/monitored_source/source_signal，无 RLS，**只存共享低风险绿色事实、零个人数据**（ADR-003）。
- PII/rights zone：contact_point/具名决策人——**列级加密或 Tokenization**（收口⑥交付）+ 保留期 + 删除链。
- **DB 角色拆分**（收口⑥）：现状 `app_user` 对 public schema 全表默认 CRUD → 拆 `tenant_app`/`platform_worker`/`platform_reader`/`migration_owner`。
- **逻辑 Schema 写入 Owner 分区**（吸收交付包 §6.2，演进方向非立即迁移）：单库内按域分逻辑区并绑定写入 Owner（示意：core/knowledge/data/intel/execution/analytics/ops），以 Repository+应用服务+DB 权限+Code Owner 共同强制「同库也禁止跨模块直接写表」。触发时机=B 写路径进场或第二个领域模块开建。

## 4. 数据流（两条）

**前向主链**：SaaS JWT → API Command → Seller Understanding →（人审）SellerProfile → ICPVersion + QueryPlan gate → ToolBroker → Provider Adapter → Raw Observation → Identity/Evidence 归一 → CandidateAssessment(ICP×Org) → Fit → 富集 → Signal/DemandProof → Contact + Storage Policy → 渠道验证 → 版本化评分 → **QualifiedLeadHandoff** → Outbox sink+ACK → SaaS 拥有 outreach/Opportunity → outcome 标签回流 backtest。

**共享源流（ingest once, project many，✅ 收口⑤已落，PR #56）**：全局公开源（TED/FDA）**平台级采一次** → `source_signal`（无 RLS 绿色事实，payload 白名单零个人数据）→ 按 subjectKey（与租户 dedupeKey 同规范化）+ taxonomy scope（`cpv:`/`fda:` 前缀键）匹配受影响租户的 ICP → 租户级投影（RLS；CC BY 署名/FDA disclaimer 在租户侧履行）→ 评分消费。**ingest-once 键** = `(provider, queryFingerprint, windowKey)`：指纹=规范化查询参数 sha256（码/国别排序去重、大小写无关），**时间窗=6h UTC 对齐桶**（env `SIGNAL_INGEST_WINDOW_MS`）——「同一外部源同一时间窗跨 workspace 只拉取一次」的可测定义，账本表 `signal_ingest`（ERROR 条件记账绝不覆盖并发 OK 行）。投影零出网；复算入口 `IntentRecomputeService`（surfaces=与增量投影同过滤面，防跨 CPV/跨 ICP 注入）；sweep 编排四段=枚举→逐 ICP 确定性解析（零出网）→指纹全局去重拉取一次→逐 ICP 只读投影。

## 5. 状态机（收敛版）

- CompanyProfile：DRAFT→ENRICHING→REVIEW→ACTIVE
- Claim：INGESTED→EXTRACTED→NEEDS_REVIEW→APPROVED→EXPIRED|REVOKED
- ICP：DRAFT→HYPOTHESIS→VALIDATING→ACTIVE→SUPERSEDED|ARCHIVED
- QueryPlan：DRAFT→READY→EXECUTED|SUPERSEDED · DiscoveryRun：RUNNING→DONE|PARTIAL|FAILED|CANCELLED
- CandidateAssessment/Lead：DISCOVERED→EVALUATING→NEEDS_REVIEW|QUALIFIED|REJECTED|SUPPRESSED→EXPIRED
- ContactPoint：UNVERIFIED→VALID|RISKY|INVALID→STALE|REVOKED · Signal：DETECTED→ACTIVE→EXPIRED|REVOKED（✅ 收口⑤：官方 API 源摄取即 ACTIVE、DETECTED 为保留态；EXPIRED 由 sweep 按 `expiresAt=occurredAt+类型 TTL`（招标 90d/清关 365d，env 可调）翻转，投影/复算剔除；REVOKED=合规撤回终态且**撤即脱敏**（subjectName 占位+payload 清空），单条/按主体/按 provider 三入口。**两级撤停语义**：source_policy SUSPENDED=停采不停用（只拦出网），「采集本身被判违规」类事件用 `revokeByProvider` 处置存量信号）
- 🔴 `CONTACTED/CONVERTED` 不由本仓自行推进——只能由 SaaS 结果事件回写（现有 Lead 状态机中这两态标记为「回写位」）。Opportunity 状态机整体在 SaaS。

## 6. 事件目录与 LeadQualifiedPackage

**本仓发布**：CompanyProfileActivated · ClaimApproved/Revoked · ICPActivated · DiscoveryRunCompleted/PartiallyCompleted · CompanyResolved · IntentSignalDetected/Expired · ContactPointVerified · **LeadQualified** · LeadQualificationRevoked · LeadSuppressed · LeadDecisionRecorded。

**LeadQualified 不可变交付快照**（幂等 ID + schema version）：

```
lead_id / workspace_id / initiative_id? / icp_id + icp_version
company_ref（canonical id + 标识符集）/ contact_refs
scores: { fit, role, intent, demand_proof?, reachability, data_quality, engagement }
evidence_refs + freshness / qualification_rule_version
storage_rights_decision / personal_data_class / suppression_state
recommended_action / valid_until
```

scores 与 as-built 六维映射注记：现行六维=fit/role/intent/dataQuality/reachability/engagement（scoring.ts）；`demand_proof` ✅ 收口⑤起由一等 Signal 填充（需求证据类事件=TENDER_PUBLISHED+SOURCING_OPENED，evidence 判据强制；FDA_CLEARANCE 属上市时机留 Intent 维）——**观测维不进总分**（乘法门待 R2 backtest ≥50 QGO 标签），`qualification_rule_version` 升 `additive-6dim-v2` 供消费端区分；**v1 契约预留 number|null 槽位使填充零破坏，snapshot_version 保持 1、不开 v2 文件**（避免 v1/v2 混流坑消费者——release-plan「升 v2」以此达成）。null=lead 评分早于 v2 规则。

**SaaS 回传（仅作学习标签）**：QgoCreated · SalesAccepted · CommercialOutcomeVerified · LeadOutcomeRejected。

**投递语义（✅ 收口③已落地，PR #46）**：事件注册表三分支（`relay/event-registry.ts` 穷举）——内部 workflow command（3 种→Temporal，AlreadyStarted 幂等）/外部 integration event（8 种→`outbox_delivery` 账本单事务原子路由，`publishedAt` 语义=已路由进交付层）/未注册类型 `parkedAt` 停靠+大声报错（不假发布、不毒化轮询）。sink：`saas` 拉模式（`GET /events` 游标=**交付账本行 id**、任意重放 at-least-once、`POST /events/ack` 幂等且锁死 pull sink）+ `webhook` 推模式（URL+SECRET 且 https 才启用；HMAC-SHA256 签名、指数退避封顶 1h、10 次 DEAD=DLQ、双路径 CAS）。LeadQualified payload=快照 v1（`packages/contracts/events/payloads/lead-qualified.v1.schema.json`，ajv Consumer Test；contact_refs 只带 ref+职务元数据不嵌 PII，含具名 refs 事件分级 RESTRICTED）。**部署约束**：relay 单写者（多副本前需 advisory lock）；其余 7 种 integration 事件的专用 payload schema 待补（现走 envelope 通契约）。

## 7. API ownership

- 保留：`/companies` `/icps` `/query-plans` `/discovery-runs` `/canonical-companies` `/leads` `/suppressions`（+补 `/events` 拉取、`/evidence` 查询、`/deletion-requests`）。
- `/campaigns` `/outreach` `/qgos` `/opportunities` **不属于本仓，任何时候不在本仓新增**；改边界唯一途径=修订 ADR-001+三方书面确认。
- Site Builder 当前端点位于 `/site-builder/intake`、`/site-builder/sites*`、`/site-builder/assets*`、`/site-builder/builds*`；正式形状只认 code-first OpenAPI。目标契约草案 [07-api-contract-draft.md](../site-builder/07-api-contract-draft.md) 与 as-built 不同的部分必须显式标注，不能冒充已实现。
- 统一返回信封 **✅ 收口④已定稿（PR #48）**：2xx 一律 `{data}`；分页 `{data, page:{next_cursor, has_more}}`；错误 `{error:{code,message,details?}}`；`/health*` 探针例外。协议键 snake_case、资源字段 camelCase；运行时与 swagger 声明同源（`common/envelope.ts` + `common/api-envelope.decorator.ts`）。契约唯一真值=code-first 导出的 `packages/contracts/openapi/openapi.json`（40 paths），CI contracts job 三道门（drift/spectral/oasdiff breaking + `breaking-change-approved` label 放行）。Evidence/Quality/Rights/Freshness/Cost/Partial 等信封扩展字段待收口⑤⑥随一等 Signal/权利词表补充。

## 8. as-built 缺口登记（已核验，8 项）

| #   | 缺口                                                                                                                                                                                                                                                                                          | 证据                                              | 处置                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | ~~Fit 挂错聚合根（canonical_company 而非 ICP×公司，多 ICP 互相覆盖）~~                                                                                                                                                                                                                        | ~~schema.prisma:504~~                             | ✅ **已修（PR #43）**：fit_verdict/fit_reasons 迁到 Lead（ICP×公司），共享 upsertLeadFit，真库真 RLS 实测两 ICP 独立互不覆盖                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2   | ~~ToolBroker 非唯一闸门：主链直调 adapter；source_policy 未登记默认放行（fail-open）；BudgetLedger.open 零调用；allowedTools 全空；伪 workspace 'discovery' 令 AI trace 静默写入失败~~                                                                                                        | ~~tool-broker.ts:97、discovery.activities.ts:99~~ | ✅ **已修（PR #51，收口②完成）**：13 个 L0 工具收编 22 处直连出网（发现/富集/intent/采集/理解五链全经 `broker.invoke`，例外四类登记：robots.txt/DNS/模型网关/outbox webhook）；`sourcePolicy=required\|advisory\|none` 分层 fail-closed（未登记/无 reader 拒；用途门按本次调用 purpose 判）+ 8 治理域 seed（algolia.net 如实 REVIEWED_RESTRICTED）；预算真开账（run 开/关账 + sweep 阶段账引用计数 + **LLM 网关 reserve-then-settle**，settle 按 token 折算、截断显性化 run 转 PARTIAL）；allowedTools 4 任务填实 + taskContractId 绑定；ExecutionContext 贯穿灭 'discovery'/'taxonomy' 伪 workspace——AI trace 真库实证写入成功。真库 verify 15 断言 + TED E2E 全绿；对抗复审 11 findings 全修         |
| 3   | ~~Outbox 假发布：LeadQualified 等无 sink 仍标 published——无真实对外交付能力~~                                                                                                                                                                                                                 | ~~outbox-relay.service.ts:143~~                   | ✅ **已修（PR #46）**：事件注册表三分支 + `outbox_delivery` 账本 + `GET /events` 拉取/ACK + 快照 v1 契约；真库 RLS 实测 24 断言 + 对抗复审 13 findings 全修                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 4   | ~~OpenAPI 双事实源：38-path JSON vs 旧 3-path YAML，contracts 5 脚本全读旧 YAML~~                                                                                                                                                                                                             | ~~packages/contracts/package.json~~               | ✅ **已修（PR #48）**：旧 YAML 删除、5 脚本切 code-first openapi.json（40 paths）；统一信封定稿（2xx 一律 `{data}`/`{data,page:{next_cursor,has_more}}`，/health 探针例外）38 业务操作全套 + swagger 响应 schema 0 缺失；CI contracts job 三道门（drift=git status porcelain + spectral + oasdiff breaking，label 放行）；对抗复审 10 findings 全修                                                                                                                                                                                                                                                                                                                                                    |
| 5   | ~~Intent 是 JSON 投影非一等事实；外部源按 ICP 重复拉取~~                                                                                                                                                                                                                                      | ~~attributes.intent.events[]~~                    | ✅ **已修（PR #56，收口⑤完成）**：`source_signal` 一等信号表（零个人数据白名单 payload、双时间轴 occurred/observed、状态机+TTL、ADR-006 字段全落）+ `signal_ingest` ingest-once 账本（指纹×6h 窗唯一键）；TED/openFDA 投影反转为**只读平台表**（fetch 拆层，构造去 broker）；sweep 四段化；`attributes.intent` 降为可复算投影（recompute surfaces=与增量同过滤面+不动点回归锁；mergeIntent 去重键 epoch 归一——存量旧格式事件一次重写自然收敛无需 backfill）；快照 demand_proof 真值填充（v1 槽位零破坏，rule→additive-6dim-v2）。真库实测：24 条真招标一次拉取、双租户各投 18 家且投影零出网、EXPIRED/REVOKED 剔除+撤即脱敏、复算重建后 unchanged；对抗复审 3 维 21 agent 14 缺陷全修/记档、2 误报驳回 |
| 6   | 实体解析单键选择非身份图（无 merge/split/回放）                                                                                                                                                                                                                                               | identity.ts:45                                    | ADR-007；R2 落最小版（R3 新 provider 前置）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 7   | ✅ **大部落地（收口⑥ 完成，PR-A #60 + PR-B）**：一等 LIA/Art.14/deletion + `DataRightsService` 判定引擎 + PII 列级加密 + DB 角色拆分（app_user 审计表 append-only + REVOKE）+ deletionWorkflow(Art.17)。**剩**：consent(Art.21)/retention sweep/细粒度 RolesGuard（roles→scopes 执行点）随 R1 | RLS migration :22                                 | ~~收口⑥~~ ✅；consent/retention/roles→scopes 归 R1（roles 执行点归 B）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 8   | 下游跨-sweep 游标饿死：扫描集不收缩，预算位次后的 match 公司永久饿死                                                                                                                                                                                                                          | roadmap 🟠（水位列修复进行中）                    | R2 验收：预算位次后公司 N 轮 sweep 内可被处理                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
