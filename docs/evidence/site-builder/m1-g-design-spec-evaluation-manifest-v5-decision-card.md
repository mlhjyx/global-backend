# M1-g `design_spec` v5 zero-cost manifest decision card

- Preparation contract: `site-builder-design-spec-evaluation-manifest-prep/2026-08-03-v2`.
- Fixed mainline source: `377f8a3ae983bad0e4ae43f767a4bc59d8f7d0a9` (`test(site-builder): add remaining task evaluation suites (#276)`).
- Machine artifact: [manifest v5](m1-g-design-spec-evaluation-manifest-v5.json), semantic SHA-256 `bcc0ac261f56a5c950e11483a3dc28f33ed678c626891367a45b6c1f56429dc4`.
- Suite/source bundle: `site-builder.design-spec-evaluation-suite/2026-08-03-v15` / `0a14c446ddb0527204b6c0a472597403aaf61998c1d12975595ae921ffd8e98d`.

The create-only writer rebuilt the ignored contracts runtime, captured the post-build
suite import receipt, compared every source-bundle file with its `377f8a3` Git blob,
and wrote the new JSON once. The frozen matrix remains one GPT-5.5 capability probe,
72 target executions, at most 146 physical wire calls, and 24 deterministic
zero-wire comparator cases. MiniMax, Doubao, Gemini, image, video, runtime routing,
promotion, and all other text tasks are excluded.

This artifact did not read credentials, balance, `.env`, or a price directory; it
made zero network/model calls and records zero model cost with
`dispatchAuthorization=NOT_AUTHORIZED`. It replaces v4 only as the current
`design_spec` fixed-source input after #276; v1–v4 remain audit artifacts. A future
fee card must bind this exact manifest, use OpenOx public pricing, and remain a
separate zero-call PR. Real evidence still requires an exact finite credential,
verifiable balance/limit, known settlement, and a separately accepted task-specific
cost authorization.
