# 兼容入口：唯一受支持的应用模板是 `apps/api/.env.example`。
# 标准 API 与 Temporal worker 服务均从 `apps/api` 启动，并读取 `apps/api/.env`。
# 请复制 `apps/api/.env.example` 为 `apps/api/.env` 后填入本机值；不要提交真实密钥。
# 保持这一文件为指针，避免根模板与应用模板发生配置漂移。
